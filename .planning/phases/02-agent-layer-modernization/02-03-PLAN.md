---
phase: 02-agent-layer-modernization
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - agents/architect.md
  - agents/security-auditor.md
  - agents/executor.md
  - agents/writer.md
  - agents/test-writer.md
  - agents/doc-writer.md
  - agents/reviewer.md
  - agents/researcher.md
autonomous: true
requirements:
  - AGENT-07
user_setup: []

must_haves:
  truths:
    - "All 8 v1.x agents pass scripts/check-frontmatter.sh."
    - "@architect and @security-auditor have effort: xhigh (bumped from high)."
    - "@executor, @writer, @test-writer, @doc-writer have explicit effort: high."
    - "@researcher stays at effort: high (NOT xhigh — exception per CONTEXT D-20)."
    - "@reviewer stays as v1.x compatibility shim with deprecation note in body."
    - "Every modernized agent has a ## Connects to section."
  artifacts:
    - "agents/architect.md, agents/security-auditor.md, agents/executor.md, agents/writer.md, agents/test-writer.md, agents/doc-writer.md, agents/reviewer.md, agents/researcher.md (8 modernized files)."
  key_links:
    - "scripts/check-frontmatter.sh exits 0 against the full agents/ directory."
---

<objective>
Modernize all 8 v1.x agents to the locked v2 frontmatter convention. Bump `@architect` and `@security-auditor` to `effort: xhigh` (read-only design/audit). Add explicit `effort: high` to code-writing agents that lack it. Add a `## Connects to` section to every agent body. Mark `@reviewer` as deprecated (v1.x compat) with a pointer to `@spec-reviewer`/`@code-reviewer`. Linter must exit 0 against the full set.

Purpose: AGENT-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-agent-layer-modernization/02-CONTEXT.md
@.planning/phases/02-agent-layer-modernization/02-01-PLAN.md

@agents/architect.md
@agents/security-auditor.md
@agents/executor.md
@agents/writer.md
@agents/test-writer.md
@agents/doc-writer.md
@agents/reviewer.md
@agents/researcher.md
@scripts/check-frontmatter.sh
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Modernize @architect (xhigh + Connects-to)</name>
  <files>agents/architect.md</files>
  <read_first>agents/architect.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md (D-13)</read_first>
  <action>Edit agents/architect.md frontmatter: change `effort: high` to `effort: xhigh`. Insert a `## Connects to` section immediately after the H1 title. Bullets:
  - **Upstream:** spawned by skills that need design work (currently `/refactor`; v2.1 may extend to `/brief` for architectural briefs)
  - **Downstream:** writes design notes (returned inline; orchestrator may persist to `.planning/spikes/<topic>.md`)
  - **Reads from:** existing codebase + .planning/research/ARCHITECTURE.md if relevant

Add a one-line note at top of body: `**Effort:** xhigh — design work, mechanically read-only via disallowedTools.`</action>
  <verify>grep -q '^effort: xhigh$' agents/architect.md
grep -q '^## Connects to' agents/architect.md
bash scripts/check-frontmatter.sh agents/architect.md 2>&1 | grep -i architect && echo "FAIL" || echo "OK"</verify>
  <acceptance_criteria>
    - "agents/architect.md has effort: xhigh"
    - "agents/architect.md has ## Connects to with Upstream + Downstream bullets"
  </acceptance_criteria>
  <done>@architect modernized, linter clean.</done>
</task>

<task type="auto">
  <name>Task 3.2: Modernize @security-auditor (xhigh + Connects-to)</name>
  <files>agents/security-auditor.md</files>
  <read_first>agents/security-auditor.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md (D-14)</read_first>
  <action>Same pattern as Task 3.1. Bump `effort: high` → `effort: xhigh`. Add `## Connects to`:
  - **Upstream:** spawned by skills that need security review (currently `/refactor`, `/build` per-task in v2.1; not yet wired in v2.0)
  - **Downstream:** returns audit findings inline; orchestrator may file to .planning/phases/*/SECURITY-REVIEW.md
  - **Reads from:** existing codebase + dependency manifests + .env / config files if accessible

Add `**Effort:** xhigh — audit work, mechanically read-only.`</action>
  <verify>grep -q '^effort: xhigh$' agents/security-auditor.md
grep -q '^## Connects to' agents/security-auditor.md
bash scripts/check-frontmatter.sh agents/security-auditor.md 2>&1 | grep -i security && echo "FAIL" || echo "OK"</verify>
  <acceptance_criteria>
    - "agents/security-auditor.md has effort: xhigh"
    - "agents/security-auditor.md has ## Connects to"
  </acceptance_criteria>
  <done>@security-auditor modernized.</done>
</task>

<task type="auto">
  <name>Task 3.3: Modernize @executor (effort + Connects-to)</name>
  <files>agents/executor.md</files>
  <read_first>agents/executor.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md (D-15)</read_first>
  <action>Add `effort: high` to frontmatter (currently missing — defaults to medium). Add `## Connects to`:
  - **Upstream:** /build N (Phase 4 WORKFLOW-06)
  - **Downstream:** writes per-task SUMMARY.md + atomic commit; downstream consumer is @code-reviewer per task
  - **Reads from:** PLAN.md (the per-task action + read_first + acceptance_criteria)

Already has `isolation: worktree`, `maxTurns: 100`, model `opus`. Note in body: `**Effort:** high — code-writing, NOT xhigh per CR-01 (rule-skipping pitfall).`

Lightly note in body that the `/execute` v1.x skill is being phased out — Phase 4 wires `/build` to spawn @executor. Don't rewrite the body; just add a one-line deprecation pointer in `## Connects to`.</action>
  <verify>grep -q '^effort: high$' agents/executor.md
grep -q '^## Connects to' agents/executor.md
bash scripts/check-frontmatter.sh agents/executor.md 2>&1 | grep -i executor && echo "FAIL" || echo "OK"</verify>
  <acceptance_criteria>
    - "agents/executor.md has effort: high"
    - "agents/executor.md has ## Connects to with /build N as Upstream"
  </acceptance_criteria>
  <done>@executor modernized.</done>
</task>

<task type="auto">
  <name>Task 3.4: Modernize @writer (effort + Connects-to)</name>
  <files>agents/writer.md</files>
  <read_first>agents/writer.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md (D-16)</read_first>
  <action>Add `effort: high`. Add `## Connects to`:
  - **Upstream:** /refactor (general-purpose code edits) and any skill that wants isolated worktree work
  - **Downstream:** returns a branch with quality-gated changes
  - **Reads from:** existing source + any reference docs the spawning skill provides

Note overlap with @executor in body: `@writer is general-purpose code editing; @executor is PLAN.md-task-aware for /build N. Phase 4 may reconsider whether both are needed.`</action>
  <verify>grep -q '^effort: high$' agents/writer.md
grep -q '^## Connects to' agents/writer.md
bash scripts/check-frontmatter.sh agents/writer.md 2>&1 | grep -i writer | grep -v test-writer | grep -v doc-writer && echo "FAIL" || echo "OK"</verify>
  <acceptance_criteria>
    - "agents/writer.md has effort: high"
    - "agents/writer.md has ## Connects to"
  </acceptance_criteria>
  <done>@writer modernized.</done>
</task>

<task type="auto">
  <name>Task 3.5: Modernize @test-writer (Connects-to)</name>
  <files>agents/test-writer.md</files>
  <read_first>agents/test-writer.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md (D-17)</read_first>
  <action>Already has `effort: high`. Just add `## Connects to`:
  - **Upstream:** /tdd (RED-GREEN-REFACTOR cycle) and /build N when the per-task action calls for tests
  - **Downstream:** writes test files alongside source; commits atomic
  - **Reads from:** the source-under-test + existing test patterns in the project</action>
  <verify>grep -q '^## Connects to' agents/test-writer.md
bash scripts/check-frontmatter.sh agents/test-writer.md 2>&1 | grep -i test-writer && echo "FAIL" || echo "OK"</verify>
  <acceptance_criteria>
    - "agents/test-writer.md has ## Connects to"
  </acceptance_criteria>
  <done>@test-writer modernized.</done>
</task>

<task type="auto">
  <name>Task 3.6: Modernize @doc-writer (Connects-to)</name>
  <files>agents/doc-writer.md</files>
  <read_first>agents/doc-writer.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md (D-18)</read_first>
  <action>Already has `effort: high`. Add `## Connects to`:
  - **Upstream:** /refactor when documentation cleanup is part of the change; /ship may invoke for README/CHANGELOG updates
  - **Downstream:** writes / edits documentation files (README, CHANGELOG, docstrings, comments)
  - **Reads from:** existing docs + source code being documented</action>
  <verify>grep -q '^## Connects to' agents/doc-writer.md
bash scripts/check-frontmatter.sh agents/doc-writer.md 2>&1 | grep -i doc-writer && echo "FAIL" || echo "OK"</verify>
  <acceptance_criteria>
    - "agents/doc-writer.md has ## Connects to"
  </acceptance_criteria>
  <done>@doc-writer modernized.</done>
</task>

<task type="auto">
  <name>Task 3.7: Modernize @reviewer (deprecation note + Connects-to)</name>
  <files>agents/reviewer.md</files>
  <read_first>agents/reviewer.md, agents/spec-reviewer.md, agents/code-reviewer.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md (D-19)</read_first>
  <action>Already has model `sonnet`, `effort: high`. Add `## Connects to`:
  - **Status:** Deprecated in v2.0; split into @spec-reviewer (pre-execution) and @code-reviewer (post-execution). Kept for v1.x skill compatibility until Phase 4 deprecates the v1.x skills.
  - **Upstream:** v1.x skills that referenced @reviewer (currently the /execute skill; deprecated in Phase 4)
  - **Downstream:** none in v2 — workflow uses @spec-reviewer and @code-reviewer

Add a deprecation banner near the top of body: `> **DEPRECATED in v2.0** — use @spec-reviewer for pre-execution scope review and @code-reviewer for post-execution code review. This file is kept for v1.x skill compatibility until Phase 4 retires the v1.x skill chain.`</action>
  <verify>grep -q '^## Connects to' agents/reviewer.md
grep -q 'DEPRECATED in v2' agents/reviewer.md
bash scripts/check-frontmatter.sh agents/reviewer.md 2>&1 | grep -i reviewer | grep -v spec-reviewer | grep -v code-reviewer && echo "FAIL" || echo "OK"</verify>
  <acceptance_criteria>
    - "agents/reviewer.md has ## Connects to"
    - "agents/reviewer.md contains 'DEPRECATED in v2.0'"
  </acceptance_criteria>
  <done>@reviewer modernized as v1.x compat shim with clear deprecation pointer.</done>
</task>

<task type="auto">
  <name>Task 3.8: Modernize @researcher (Connects-to; effort stays high)</name>
  <files>agents/researcher.md</files>
  <read_first>agents/researcher.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md (D-20)</read_first>
  <action>Frontmatter currently has `model: sonnet`, `background: true`. Add `effort: high` if missing. Do NOT bump to xhigh (per CONTEXT D-20 exception — research is shallow-many, not deep design).

Add `## Connects to`:
  - **Upstream:** /brief N (when CONTEXT-gathering needs research) and /explore-repo
  - **Downstream:** returns findings inline; orchestrator may persist to .planning/research/<topic>.md
  - **Reads from:** existing codebase + WebFetch/WebSearch for external research</action>
  <verify>grep -q '^effort: high$' agents/researcher.md
grep -q '^## Connects to' agents/researcher.md
bash scripts/check-frontmatter.sh agents/researcher.md 2>&1 | grep -i researcher && echo "FAIL" || echo "OK"</verify>
  <acceptance_criteria>
    - "agents/researcher.md has effort: high (NOT xhigh — exception per CONTEXT D-20)"
    - "agents/researcher.md has ## Connects to"
  </acceptance_criteria>
  <done>@researcher modernized; effort stays at high per documented exception.</done>
</task>

</tasks>

<verification>
- [ ] All 8 v1.x agent files modernized with frontmatter + ## Connects to
- [ ] `bash scripts/check-frontmatter.sh` exits 0 against the FULL agents/ directory (8 v1.x + 4 new = 12 agents)
- [ ] @architect and @security-auditor at effort: xhigh
- [ ] @researcher at effort: high (exception)
- [ ] @reviewer carries deprecation note
- [ ] 8 atomic commits (one per agent)
</verification>

<success_criteria>
- AGENT-07 closed (all 8 v1.x agents modernized)
- Linter passes against the full set
- Connects-to chains complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-layer-modernization/02-03-SUMMARY.md`.
</output>