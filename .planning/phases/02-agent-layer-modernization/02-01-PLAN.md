---
phase: 02-agent-layer-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-frontmatter.sh
  - rules/godmode-routing.md
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-06
user_setup: []

must_haves:
  truths:
    - "scripts/check-frontmatter.sh exists, is executable, and is shellcheck-clean."
    - "Linter refuses any agent missing required fields (name, description, model, effort, tools, ## Connects to section)."
    - "Linter refuses any agent with model: matching `^claude-` (pinned IDs)."
    - "Linter refuses any agent with effort: xhigh combined with Write or Edit in tools without disallowedTools containing both."
    - "rules/godmode-routing.md documents the new convention (effort tier policy + Connects-to format)."
  artifacts:
    - "scripts/check-frontmatter.sh exists, exit 0 when run against compliant agents."
    - "rules/godmode-routing.md contains an `## Effort Tier Policy` section."
    - "rules/godmode-routing.md contains a `## Connects-to Convention` section."
  key_links:
    - "scripts/check-frontmatter.sh consumed by Phase 3 PreToolUse hook (HOOK-01) and Phase 5 CI (QUAL-01)."
    - "rules/godmode-routing.md is the authoritative source for the linter rules; linter messages reference it."
---

<objective>
Ship the frontmatter convention as both a documented rule (rules/godmode-routing.md) and a mechanical linter (scripts/check-frontmatter.sh). After this plan, a malformed agent file fails CI; a well-formed agent file passes silently. Phase 3 wires the linter into the PreToolUse hook; Phase 5 wires it into GitHub Actions. This plan ships the linter substrate.

Purpose: AGENT-01 (frontmatter convention locked), AGENT-02 (effort tier policy enforced), AGENT-06 (linter ships as pure-bash CI script).

Output:
- New: `scripts/check-frontmatter.sh` (~150 lines bash + grep + jq)
- Edited: `rules/godmode-routing.md` (add 2 sections; light edit)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-agent-layer-modernization/02-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md

@rules/godmode-routing.md
@agents/architect.md
@scripts/check-version-drift.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Update rules/godmode-routing.md with effort tier policy + Connects-to convention</name>
  <files>rules/godmode-routing.md</files>
  <read_first>rules/godmode-routing.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md</read_first>
  <action>Append two new sections to `rules/godmode-routing.md` (or insert near other agent-routing content if a logical home exists). Sections:

```markdown
## Effort Tier Policy (locked 2026-04-26 — Phase 2)

Every agent declares `effort: high` OR `effort: xhigh` in its frontmatter. No `medium`, `low`, or blank.

**Code-writing agents (effort: high):** `@executor`, `@writer`, `@test-writer`, `@doc-writer`, `@code-reviewer`. These agents have `Write` and/or `Edit` in their `tools:` field. They lock at `effort: high` because Opus 4.7's `xhigh` is documented to skip rule adherence — too risky when an agent is mutating source files.

**Design / audit agents (effort: xhigh):** `@architect`, `@security-auditor`, `@planner`, `@verifier`, `@spec-reviewer`. These agents declare `disallowedTools: Write, Edit` (mechanically read-only). They use `xhigh` for deeper analysis on read-only work where rule-skipping has no source-modifying consequences.

**Exception:** `@researcher` stays at `effort: high` despite being read-only. Research is shallow-many (lots of small lookups, web fetches), not deep design — `xhigh` wastes tokens on this workload.

The frontmatter linter (`scripts/check-frontmatter.sh`) refuses commits where `effort: xhigh` is combined with `Write` or `Edit` in `tools:` and `disallowedTools` does NOT contain both. This is the mechanical enforcement of the policy.

## Connects-to Convention (locked 2026-04-26 — Phase 2)

Every agent body has a `## Connects to` H2 section near the top (after the H1 title, before the main process description). Bullets describe upstream / downstream relationships:

```markdown
## Connects to
- **Upstream:** /skill-name (the skill that spawns this agent)
- **Downstream:** Writes <artifact> consumed by /next-skill or @next-agent
- **Reads from:** <upstream artifact path>
```

`/godmode` (Phase 4) renders this section by `grep -A 20 '^## Connects to' agents/*.md` and presents the chain to the user. Power users can modify any agent without breaking the indexer — the convention is the contract.

The linter asserts every agent has a `## Connects to` section with at least one `**Upstream:**` line and at least one `**Downstream:**` line.
```

Notes:
- Don't replace existing routing content — append/insert. The existing `effort: high` reference for `@executor`/`@writer` (already in this rule file from prior CLAUDE.md) stays consistent with the new policy section.
- Use the literal `## Effort Tier Policy (locked 2026-04-26 — Phase 2)` heading so the linter messages can reference the rule by section name.</action>
  <verify>grep -c '^## Effort Tier Policy' rules/godmode-routing.md # → 1
grep -c '^## Connects-to Convention' rules/godmode-routing.md # → 1
grep -q '@researcher.*effort: high' rules/godmode-routing.md # → exit 0
grep -q 'effort: xhigh.*disallowedTools' rules/godmode-routing.md # → exit 0</verify>
  <acceptance_criteria>
    - "rules/godmode-routing.md contains '## Effort Tier Policy (locked 2026-04-26 — Phase 2)'"
    - "rules/godmode-routing.md contains '## Connects-to Convention (locked 2026-04-26 — Phase 2)'"
    - "Both sections describe the rule, the rationale, and the mechanical enforcement"
  </acceptance_criteria>
  <done>Authoritative rule documentation lands; linter messages can reference it.</done>
</task>

<task type="auto">
  <name>Task 1.2: Create scripts/check-frontmatter.sh</name>
  <files>scripts/check-frontmatter.sh</files>
  <read_first>scripts/check-version-drift.sh, agents/architect.md, .planning/phases/02-agent-layer-modernization/02-CONTEXT.md</read_first>
  <action>Create `scripts/check-frontmatter.sh` as a pure bash + grep + jq linter. Walks `agents/*.md`, parses frontmatter, applies 6 checks per CONTEXT D-22, exits non-zero on any failure.

```bash
#!/usr/bin/env bash
# scripts/check-frontmatter.sh
# Linter for agents/*.md frontmatter. Pure bash + grep + jq. Bash 3.2 portable.
# Exit 0 on success; exit 1 on any failure with file:rule:evidence on stderr.
# CI gate (Phase 5 wires this into GitHub Actions; Phase 3 wires it into PreToolUse hook).
# Convention authority: rules/godmode-routing.md ## Effort Tier Policy + Connects-to Convention.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
AGENTS_DIR="$REPO_ROOT/agents"

[ -d "$AGENTS_DIR" ] || { echo "[x] agents/ directory not found at $AGENTS_DIR"; exit 1; }
command -v grep >/dev/null 2>&1 || { echo "[x] grep required"; exit 1; }

FAILED=0
report_fail() {
  echo "[!] $1: $2: $3" >&2
  FAILED=$((FAILED + 1))
}

# Extract value of a YAML scalar key from frontmatter block.
# Usage: yaml_get FILE KEY
# Returns: the value (everything after `KEY:` until end-of-line, trimmed) or empty if not found.
yaml_get() {
  local file="$1" key="$2"
  awk -v key="$key" '
    /^---$/ { count++; if (count == 2) exit; next }
    count == 1 && match($0, "^"key"[[:space:]]*:[[:space:]]*") {
      val = substr($0, RLENGTH + 1)
      gsub(/^[[:space:]]+|[[:space:]]+$/, "", val)
      print val
      exit
    }
  ' "$file"
}

# Check if a file has a key declared anywhere in the frontmatter (presence test, not value).
yaml_has() {
  local file="$1" key="$2"
  awk -v key="$key" '
    /^---$/ { count++; if (count == 2) exit; next }
    count == 1 && /^[a-zA-Z_]+[[:space:]]*:/ {
      gsub(/[[:space:]]*:.*/, "")
      if ($0 == key) { found = 1; exit }
    }
    END { exit !found }
  ' "$file"
}

# Lint a single agent file.
lint_agent() {
  local file="$1"
  local rel="${file#$REPO_ROOT/}"
  local name desc model effort tools disallowed connects_section

  # 1. name field (must match filename)
  name=$(yaml_get "$file" "name")
  expected_name="$(basename "$file" .md)"
  if [ -z "$name" ]; then
    report_fail "$rel" "missing-name" "no name: field in frontmatter"
    return
  fi
  if [ "$name" != "$expected_name" ]; then
    report_fail "$rel" "name-mismatch" "name: $name (expected $expected_name)"
  fi

  # 2. description field
  desc=$(yaml_get "$file" "description")
  [ -n "$desc" ] || report_fail "$rel" "missing-description" "no description: field"

  # 3. model field — must be alias, never pinned
  model=$(yaml_get "$file" "model")
  if [ -z "$model" ]; then
    report_fail "$rel" "missing-model" "no model: field"
  else
    case "$model" in
      opus|sonnet|haiku) ;;
      claude-*)
        report_fail "$rel" "pinned-model-id" "model: $model (use alias: opus|sonnet|haiku)"
        ;;
      *)
        report_fail "$rel" "invalid-model" "model: $model (must be opus|sonnet|haiku)"
        ;;
    esac
  fi

  # 4. effort field — must be high or xhigh
  effort=$(yaml_get "$file" "effort")
  case "$effort" in
    high|xhigh) ;;
    "")
      report_fail "$rel" "missing-effort" "no effort: field (must be high or xhigh per godmode-routing.md)"
      ;;
    *)
      report_fail "$rel" "invalid-effort" "effort: $effort (must be high or xhigh)"
      ;;
  esac

  # 5. tools field
  tools=$(yaml_get "$file" "tools")
  [ -n "$tools" ] || report_fail "$rel" "missing-tools" "no tools: field"

  # 6. effort: xhigh + Write/Edit safety check
  disallowed=$(yaml_get "$file" "disallowedTools")
  if [ "$effort" = "xhigh" ]; then
    case "$tools" in
      *Write*|*Edit*)
        case "$disallowed" in
          *Write*Edit*|*Edit*Write*) ;;  # both present, OK
          *)
            report_fail "$rel" "xhigh-with-write" "effort: xhigh + Write/Edit in tools: but disallowedTools missing both (CR-01 — see rules/godmode-routing.md ## Effort Tier Policy)"
            ;;
        esac
        ;;
    esac
  fi

  # 7. ## Connects to section
  if ! grep -q '^## Connects to' "$file"; then
    report_fail "$rel" "missing-connects-to" "no '## Connects to' section in body (see rules/godmode-routing.md ## Connects-to Convention)"
  else
    # Extract the section's content (until next ## heading or EOF) and check for Upstream + Downstream bullets
    connects_section=$(awk '/^## Connects to/{flag=1; next} /^## /{flag=0} flag' "$file")
    case "$connects_section" in
      *"**Upstream:**"*) ;;
      *) report_fail "$rel" "connects-no-upstream" "## Connects to section missing **Upstream:** bullet" ;;
    esac
    case "$connects_section" in
      *"**Downstream:**"*) ;;
      *) report_fail "$rel" "connects-no-downstream" "## Connects to section missing **Downstream:** bullet" ;;
    esac
  fi
}

# Walk all agent files.
LC_ALL=C
for agent in "$AGENTS_DIR"/*.md; do
  [ -f "$agent" ] || continue
  case "$(basename "$agent")" in
    _*|README.md) continue ;;  # ignored prefixes
  esac
  lint_agent "$agent"
done

if [ "$FAILED" -eq 0 ]; then
  echo "[+] frontmatter clean ($(find "$AGENTS_DIR" -maxdepth 1 -name '*.md' -not -name '_*' -not -name 'README.md' | wc -l | tr -d ' ') agents checked)"
  exit 0
else
  echo "[x] $FAILED frontmatter violation(s) — see above"
  exit 1
fi
```

`chmod +x scripts/check-frontmatter.sh`.

Constraints:
- Bash 3.2 portable. `awk` is available everywhere; no `mapfile`, `[[ -v ]]`, `${var,,}`, `declare -A`.
- No `yq` dependency (PROJECT.md constraint). The awk parser handles flat scalar keys (which is all our agents use).
- `set -euo pipefail` plus `|| true` patterns where awk might return non-zero (yaml_get returning empty is normal, not failure).
- Output style matches `scripts/check-version-drift.sh` ([+]/[!]/[x] prefixes, color-friendly).</action>
  <verify>test -x scripts/check-frontmatter.sh # → executable
bash -n scripts/check-frontmatter.sh # → syntax OK
shellcheck scripts/check-frontmatter.sh # → exit 0
# Run against current v1.x agents (most should fail because they lack ## Connects to and effort)
bash scripts/check-frontmatter.sh ; echo "exit: $?" # → exits non-zero with violations
# Will pass after Plans 02-02 + 02-03 modernize all agents.</verify>
  <acceptance_criteria>
    - "scripts/check-frontmatter.sh exists and is executable"
    - "scripts/check-frontmatter.sh contains 6 lint rules: missing-name, missing-description, missing-model + pinned-model-id, missing-effort, missing-tools, xhigh-with-write, missing-connects-to + upstream/downstream bullet checks"
    - "shellcheck scripts/check-frontmatter.sh exits 0"
    - "When run on a compliant fixture (a hand-crafted minimal agent file with all required fields), exits 0"
    - "When run on the current v1.x agents (BEFORE Plans 02-02/02-03 modernize them), exits non-zero with at least 8 'missing-connects-to' violations (one per v1.x agent — Connects-to is new in v2)"
  </acceptance_criteria>
  <done>Linter ships, syntax-clean, shellcheck-clean, correctly flags v1.x non-compliance.</done>
</task>

</tasks>

<verification>
- [ ] `scripts/check-frontmatter.sh` exists, is executable, shellcheck-clean
- [ ] `bash scripts/check-frontmatter.sh` exits non-zero against v1.x agents (BEFORE 02-02/02-03 modernize them)
- [ ] `rules/godmode-routing.md` has both new sections
- [ ] 2 atomic commits (one per task)
</verification>

<success_criteria>
- AGENT-01, AGENT-02, AGENT-06 closed
- Linter is the convention enforcement mechanism for Plans 02-02 and 02-03
- Phase 3 (HOOK-01) and Phase 5 (QUAL-01) have a ready substrate to wire into
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-layer-modernization/02-01-SUMMARY.md`.
</output>
