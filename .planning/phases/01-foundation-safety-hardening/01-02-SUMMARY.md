# Plan 01-02 Summary — Hook hardening

**Executed:** 2026-04-26
**Mode:** Inline
**Result:** ✓ All 5 tasks COVERED

## What landed

| Task | Commit | Files |
|---|---|---|
| 2.1 Create config/quality-gates.txt | e8710e8 | config/quality-gates.txt (new) |
| 2.2 Harden session-start.sh (JSON-via-jq, cwd, stdin) | 36e11a3 | hooks/session-start.sh |
| 2.3 Harden post-compact.sh substrate (JSON, cwd, stdin, live FS) | adfe00e | hooks/post-compact.sh |
| 2.4 post-compact.sh reads gates from config/quality-gates.txt | eac92a3 | hooks/post-compact.sh (separate atomic commit per CONTEXT D-19) |
| 2.5 Adversarial branch-name fixtures | 0dc4261, d53c711 | tests/fixtures/hooks/* (new) |

## Closes

- FOUND-04 (Hooks emit valid JSON under adversarial inputs) ✓
- FOUND-05 (Hooks read cwd from stdin, tolerate stdin closure) ✓
- FOUND-07 (Quality-gates SoT) ✓
- FOUND-11 (Live filesystem indexing substrate) ✓
- CONCERNS #6, #7, #8, #9, #18 ✓

## Verification

5 fixtures × 2 hooks = 10/10 valid JSON outputs (`jq -e '.'` on each)
`grep '<<EOF' hooks/*.sh` → no matches
`grep '1. Typecheck passes' hooks/post-compact.sh` → no matches (gates moved to file)

## Deviations

- **macOS git rejects 2 of 5 adversarial branch names.** `feat/back\slash` and `feat/line\nbreak` cannot be created via macOS git ref validation. Setup script falls back to `main` branch with a marker note. Linux CI (Phase 5) exercises the full fixture set.
- **Bug in initial post-compact.sh substrate:** `PLUGIN_ROOT` resolution happened AFTER `cd $HOOK_CWD`, breaking script-relative paths. Fixed in same commit by amending — PLUGIN_ROOT now resolves BEFORE any cd. Lesson logged for Phase 3 hook authors.
- **Skill list formatting bug:** Initial `${SKILLS_LIST// / //}` produced doubled slashes (`//execute //explore-repo`). Fixed to comma-separated `${SKILLS_LIST// /, /}`. Same fix applied to agents.

## Generated artifacts (gitignored)

The 5 `tests/fixtures/hooks/cwd-*.json` files are generated by `setup-fixtures.sh` at test time and are no longer committed (added to `.gitignore`). The setup script is the source of truth.
