---
phase: 05-quality-ci-tests-docs-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - .claude-plugin/plugin.json
  - CONTRIBUTING.md
autonomous: true
requirements:
  - QUAL-05
  - QUAL-06
tags:
  - docs
  - changelog
  - readme
  - marketplace
  - release

must_haves:
  truths:
    - "README.md is a v2-shaped tutorial-first front door with the 9 sections per D-19 (Quick start, What you get, Installation, /godmode arrow chain, Auto Mode, Customization, Troubleshooting, FAQ, Contributing, License); wc -l README.md returns <=500."
    - "README.md contains the locked tagline 'Senior engineering team, in a plugin.' and the locked arrow chain '/godmode -> /mission -> /brief N -> /plan N -> /build N -> /verify N -> /ship' plus the helper line '+ helpers: /debug /tdd /refactor /explore-repo'."
    - "README.md passes bash scripts/check-vocab.sh (no phase, task, story, PRD, gsd-*, cycle, milestone substrings)."
    - "CHANGELOG.md has a top-of-file dated v2.0.0 heading, with Keep-a-Changelog Added/Changed/Fixed/Removed/Security taxonomy and bullets grouped by Foundation/Agents/Hooks/Skills/Quality milestone areas."
    - "CHANGELOG.md compresses v1.x history into a single trailing v1.x.x block (D-24)."
    - ".claude-plugin/plugin.json description is the locked v2 string per D-25 (under 200 chars); keywords array updated to v2 set per D-25; userConfig.model_profile from Phase 1 PRESERVED (D-26); version bumped to 2.0.0."
    - "CONTRIBUTING.md has a top-of-file pointer to README.md AND a tag-protection note near PR Process per D-29."
    - "bash scripts/check-version-drift.sh exits 0 (CHANGELOG top heading, plugin.json version, all README mentions are coherent)."
  artifacts:
    - path: "README.md"
      provides: "v2 marketing front door — tutorial-first, <=500 lines"
      contains: "Senior engineering team, in a plugin."
      max_lines: 500
    - path: "CHANGELOG.md"
      provides: "Dated v2.0.0 release entry + compressed v1.x history"
      contains: "v2.0.0"
      min_lines: 80
    - path: ".claude-plugin/plugin.json"
      provides: "Marketplace metadata polish (description, keywords, version 2.0.0, userConfig preserved)"
      contains: "Senior engineering team"
      min_lines: 25
    - path: "CONTRIBUTING.md"
      provides: "README pointer + tag-protection note"
      contains: "Tag protection"
      min_lines: 130
  key_links:
    - from: "README.md"
      to: ".claude-plugin/plugin.json"
      via: "version coherence (no hardcoded version literals)"
      pattern: "v2\\.0\\.0"
    - from: "CHANGELOG.md"
      to: ".claude-plugin/plugin.json"
      via: "version drift gate (top heading must match .version)"
      pattern: "## v2\\.0\\.0"
    - from: ".claude-plugin/plugin.json"
      to: "userConfig.model_profile"
      via: "Phase 1 single user-tunable knob — must NOT be stripped"
      pattern: "model_profile"
    - from: "CONTRIBUTING.md"
      to: "README.md"
      via: "top-of-file pointer (no duplication)"
      pattern: "README.md"
---

<objective>
Polish the user-facing docs and marketplace metadata for v2.0.0 release. This plan ships:

1. README.md rewrite to the locked 9-section skeleton (D-19) — tutorial-first, <=500 lines, locked tagline + arrow chain.
2. CHANGELOG.md dated v2.0.0 entry summarizing the 5 milestone areas in Keep-a-Changelog format (D-22..D-24).
3. .claude-plugin/plugin.json description + keywords polish (D-25), version bump to 2.0.0, userConfig.model_profile preserved (D-26).
4. CONTRIBUTING.md light touch — pointer to README + tag-protection note (D-29).

Purpose: QUAL-05 + QUAL-06. The marketing front door for the v2.0.0 release. Without this plan, the plugin marketplace listing still advertises v1.x ("8 agents, 8 skills, full feature pipeline").

Output: 4 MODIFIED files. The v2.0.0 git tag is NOT cut by this plan — per D-27 it is cut from main AFTER the Phase 5 PR merges and CI is green. This plan documents the expected tag step in 05-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-quality-ci-tests-docs-parity/05-CONTEXT.md
@.planning/phases/05-quality-ci-tests-docs-parity/05-PATTERNS.md
@.planning/research/PITFALLS.md
@./CLAUDE.md
@./IDEA.md
@README.md
@CHANGELOG.md
@.claude-plugin/plugin.json
@CONTRIBUTING.md

<interfaces>
<!-- Locked content from PROJECT.md / IDEA.md / CLAUDE.md / 05-CONTEXT.md. -->

Tagline (locked, verbatim — appears in README and plugin.json description):

  Senior engineering team, in a plugin.

Arrow chain (locked, verbatim — appears in README "What you get" and "/godmode arrow chain" sections):

  /godmode -> /mission -> /brief N -> /plan N -> /build N -> /verify N -> /ship

(NOTE: in the rendered README, render the arrow as the Unicode right-arrow character U+2192, not as ASCII -> . The interface text uses ASCII for safe transport.)

Helpers line (locked, verbatim):

  + helpers: /debug  /tdd  /refactor  /explore-repo

The 11 user-invocable skills (per 04-04-SURFACE-AUDIT.md section 1):
  1. /godmode      — orient, "what now?" in 5 lines
  2. /mission      — initialize / update PROJECT.md + ROADMAP.md
  3. /brief N      — Socratic brief: why + what + spec -> BRIEF.md
  4. /plan N       — tactical breakdown -> PLAN.md
  5. /build N      — wave-based parallel execution, atomic commits
  6. /verify N     — goal-backward verification, COVERED/PARTIAL/MISSING
  7. /ship         — quality gates, push, gh pr create
  8. /debug        — structured debug protocol
  9. /tdd          — red, green, refactor
  10. /refactor    — safe refactoring
  11. /explore-repo — read-only repo exploration

Locked README skeleton (D-19 — exactly these H1/H2 headings, in this order):

  # claude-godmode
  > Senior engineering team, in a plugin.
  ## Quick start (2-minute tutorial)
  ## What you get (one-paragraph capability summary + visual chain)
  ## Installation (plugin marketplace + manual paths, side-by-side)
  ## The /godmode arrow chain (one-line explanation per skill)
  ## Auto Mode (one paragraph)
  ## Customization (rule overrides, model_profile, skill authoring pointer)
  ## Troubleshooting (5-7 most common issues with one-line fixes)
  ## FAQ (5-7 questions)
  ## Contributing & development (one-line pointer to CONTRIBUTING.md)
  ## License (one line)

Locked plugin.json description (D-25 — verbatim, 197 chars; uses Unicode arrow U+2192):

  "Senior engineering team, in a plugin. One arrow chain (/godmode -> /mission -> /brief -> /plan -> /build -> /verify -> /ship), 11 skills, mechanical quality gates."

  In the actual JSON file, replace the ASCII -> with the Unicode arrow character U+2192. The verbatim string D-25 quoted (with U+2192) is what goes into the file.

Locked plugin.json keywords (D-25 — verbatim):

  ["workflow","agents","skills","hooks","planning","quality-gates","auto-mode","claude-code"]

Forbidden vocabulary (D-12 — must NOT appear in README, case-insensitive, word-boundary):
  phase, task, story, PRD, gsd-*, cycle, milestone

Keep-a-Changelog v1.1.0 taxonomy (D-22 — exactly these 5 sub-headings under each release):
  ### Added
  ### Changed
  ### Fixed
  ### Removed
  ### Security

Current version-drift gate behavior (scripts/check-version-drift.sh):
  - Reads .claude-plugin/plugin.json:.version as canonical.
  - Asserts CHANGELOG top heading matches: ^## v[0-9]+\.[0-9]+\.[0-9]+ line top-of-file matches canonical.
  - Asserts README.md vN.N.N mentions match canonical.

  This means: .claude-plugin/plugin.json:.version = "2.0.0" AND CHANGELOG.md first version heading must match the regex ^## v[0-9]+\.[0-9]+\.[0-9]+ — therefore the heading is "## v2.0.0 — 2026-04-28" (NO brackets), not "## [v2.0.0]".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite README.md to v2 9-section skeleton (<=500 lines, tutorial-first, vocab-clean)</name>
  <files>README.md</files>
  <read_first>
    - README.md (CURRENT — 424 lines of v1.x; full read to understand what is being replaced)
    - IDEA.md (locked tagline source + locked arrow chain)
    - CLAUDE.md (project guiding principles, locked vocabulary, 11-command surface)
    - .planning/PROJECT.md (Core Value statement; Constraints)
    - .planning/phases/05-quality-ci-tests-docs-parity/05-CONTEXT.md (D-18, D-19, D-20, D-21)
    - .planning/phases/05-quality-ci-tests-docs-parity/05-PATTERNS.md README.md section
    - commands/godmode.md (read frontmatter description)
    - skills/mission/SKILL.md, skills/brief/SKILL.md, skills/plan/SKILL.md, skills/build/SKILL.md, skills/verify/SKILL.md, skills/ship/SKILL.md (frontmatter description: line of each — used in /godmode arrow chain section)
    - skills/debug/SKILL.md, skills/tdd/SKILL.md, skills/refactor/SKILL.md, skills/explore-repo/SKILL.md (same)
    - install.sh (read first 30 lines — confirm install command syntax for Quick start tutorial)
    - .planning/research/PITFALLS.md ME-04 entry (no-duplication-with-CONTRIBUTING/CHANGELOG rule)
  </read_first>
  <action>
This is a STRUCTURAL REWRITE, not an edit. The current README.md (424 lines) is v1.x-shaped — references /prd -> /plan-stories -> /execute -> /ship, "Pipeline" as a section heading, "8 agents 8 skills", "Standalone Workflows", "Agent Memory" as a section. ALL of that vocabulary is forbidden in v2 (D-12). Rewrite from scratch to the 9-section skeleton.

Hard constraints:

1. Total line count <=500 (D-19 — including blank lines). Aim for 350-450 to leave headroom.
2. NO forbidden vocabulary: phase, task, story, PRD, gsd-*, cycle, milestone. Verified by `bash scripts/check-vocab.sh` post-write.
3. Tagline `> Senior engineering team, in a plugin.` immediately under the H1 (D-19).
4. Arrow chain rendered ASCII once in `## What you get` (D-20). Use the Unicode right-arrow character U+2192 in the rendered file:

   `/godmode → /mission → /brief N → /plan N → /build N → /verify N → /ship`
   `+ helpers: /debug  /tdd  /refactor  /explore-repo`

5. NO duplication with CONTRIBUTING.md or CHANGELOG.md (D-21, ME-04). README is marketing front door; CONTRIBUTING is dev manual.
6. NO hardcoded version literals (FOUND-02). The version is in plugin.json:.version. README does NOT print "v2.0.0" as a literal except inside a `## What is new` callout that links to CHANGELOG.md (and even there, prefer "the latest release" over hardcoding "v2.0.0").

Concrete content per section (use these exact H1/H2 headings):

```markdown
# claude-godmode

> Senior engineering team, in a plugin.

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Claude Code](https://img.shields.io/badge/Claude_Code-2.1.111+-blueviolet)](https://docs.claude.com/en/docs/claude-code)

A Claude Code plugin that makes Claude behave like a senior engineering team out of the box. One workflow chain. 11 user-facing slash commands. Mechanical quality gates. Bash 3.2 + jq only — no Node, no Python, no SDK.

## Quick start (2-minute tutorial)

(5 numbered steps — install, open Claude Code, type /godmode, run /mission, follow the chain. Concrete commands; copy-pasteable. <=30 lines.)

## What you get

(One paragraph summarizing capability — agent layer, hook gates, skill chain, parity-tested install. Then the ASCII arrow chain block. <=25 lines.)

```
/godmode → /mission → /brief N → /plan N → /build N → /verify N → /ship
+ helpers: /debug  /tdd  /refactor  /explore-repo
```

## Installation

(Side-by-side: plugin marketplace path AND manual ./install.sh path. Two H3 sub-headings. <=40 lines.)

### Plugin marketplace

(One-paragraph instructions — open Claude Code, marketplace browse, install claude-godmode.)

### Manual install

(One paragraph + code block: git clone <url>; cd claude-godmode; ./install.sh. Mentions --force, mentions backup directory.)

## The /godmode arrow chain

(One-line explanation per skill — 11 entries. NO arrow-chain diagram repeated; just the per-skill bullet list. <=30 lines.)

- `/godmode` — orient: prints a "what now?" answer in <=5 lines, reading .planning/STATE.md.
- `/mission` — initialize a project's .planning/ artifacts (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md).
- `/brief N` — Socratic brief authoring -> .planning/briefs/NN-name/BRIEF.md (why + what + spec).
- `/plan N` — tactical breakdown -> .planning/briefs/NN-name/PLAN.md (atomic items + verification).
- `/build N` — wave-based parallel execution; one atomic commit per item.
- `/verify N` — goal-backward verification; reports COVERED / PARTIAL / MISSING per success criterion.
- `/ship` — runs quality gates, pushes, opens a PR via gh pr create.
- `/debug` — structured debug protocol (helper).
- `/tdd` — red, green, refactor (helper).
- `/refactor` — safe refactoring (helper).
- `/explore-repo` — read-only repo exploration (helper).

(Note: when describing /build N, do NOT use the word "task" — say "item" or "atomic commit" instead. The vocab gate forbids `task` in README.md.)

## Auto Mode

(One paragraph — Claude Code's permission_mode: auto; what changes for the user. Skills detect "Auto Mode Active" and pick reasonable defaults rather than asking. ~10 lines.)

## Customization

(One paragraph + 3-bullet list: rule overrides via ~/.claude/rules/, model_profile knob in plugin.json's userConfig, skill authoring pointer to CONTRIBUTING.md. <=20 lines.)

## Troubleshooting

(5-7 most common issues, each with one-line fix. <=30 lines. Cover:
- bash --version shows 3.2 / older — fine, plugin is bash 3.2 portable.
- jq: command not found — install jq via brew/apt.
- Reinstall overwrote my customization — non-TTY default keeps customizations; check ~/.claude/backups/.
- Hooks not firing — verify ~/.claude/settings.json has the hooks block.
- Statusline not showing — restart Claude Code.
- /godmode says "no project found" — run /mission first.)

## FAQ

(5-7 questions. <=30 lines. Suggested:
- How is this different from other Claude Code plugins? — one workflow chain, mechanical gates, bash + jq only.
- Does it work on Windows? — WSL2 yes; native PowerShell no (out of scope).
- Can I cherry-pick individual skills? — no; the plugin is opinionated and cohesive.
- How do I upgrade from v1.x? — re-run ./install.sh; v1.x deprecation banners guide migration.
- Where do I file bugs? — link to GitHub Issues.
- Does it phone home? — never. No telemetry.
- What is the license? — MIT.)

## Contributing & development

See [CONTRIBUTING.md](CONTRIBUTING.md). (One line. NO duplication of dev manual content here.)

## License

MIT — see [LICENSE](LICENSE).
```

Word substitution table (when rephrasing the v1.x README content):

  v1.x word    -> v2 substitute
  -------------------------------
  phase        -> section, area, milestone-area (avoid; rewrite the sentence)
  task         -> item, atomic commit, work-unit
  story        -> brief
  PRD          -> brief
  gsd-*        -> (do not reference GSD anywhere in user-facing surface)
  cycle        -> round, iteration
  milestone    -> release

If a sentence reads naturally only with a forbidden word, REWRITE THE SENTENCE.

Implementation steps:
1. Read the current README.md fully.
2. Cherry-pick any v1.x content worth preserving (badges, link patterns, license note).
3. Write the new README from the skeleton above. Keep prose tight; no marketing fluff.
4. Run `bash scripts/check-vocab.sh` (the shipped check-vocab.sh from Plan 05-01 walks the whole surface tree by default; README.md is in scope). Verify zero violations on README.md.
5. Run `wc -l README.md` — must be <=500.
6. Run `bash scripts/check-version-drift.sh` — must exit 0 (no hardcoded vN.N.N outside the canonical version mention; any version mention must match plugin.json:.version which Task 3 sets to 2.0.0).
  </action>
  <verify>
    <automated>test "$(wc -l < README.md | tr -d ' ')" -le 500 && grep -q '^# claude-godmode' README.md && grep -qF '> Senior engineering team, in a plugin.' README.md && grep -q '/godmode .* /mission .* /brief' README.md && grep -q 'helpers: /debug' README.md && test "$(grep -cE '^## ' README.md)" -ge 9</automated>
  </verify>
  <acceptance_criteria>
    - File exists and <=500 lines: `[ "$(wc -l < README.md | tr -d ' ')" -le 500 ]`.
    - H1 + tagline: `head -3 README.md | grep -c '^# claude-godmode$'` returns 1; `head -10 README.md | grep -cF '> Senior engineering team, in a plugin.'` returns 1.
    - 9 H2 sections per D-19: each of `^## Quick start`, `^## What you get`, `^## Installation`, `^## The /godmode arrow chain`, `^## Auto Mode`, `^## Customization`, `^## Troubleshooting`, `^## FAQ`, `^## Contributing & development`, `^## License` returns 1 hit via grep -c (10 H2 headings total).
    - Arrow chain rendered: `grep -c '/godmode' README.md` returns >=2 (chain block + one-line bullet); chain block also includes `/mission`, `/brief N`, `/plan N`, `/build N`, `/verify N`, `/ship` on the same or adjacent lines.
    - Helpers line present: `grep -cF 'helpers: /debug' README.md` returns >=1.
    - 11 skills enumerated in arrow-chain section: count `^- \`/<name>\`` for each skill — `grep -cE "^- \`/(godmode|mission|brief|plan|build|verify|ship|debug|tdd|refactor|explore-repo)" README.md` returns 11.
    - No forbidden vocab (when scripts/check-vocab.sh exists from Plan 05-01): `bash scripts/check-vocab.sh` exits 0. Fallback grep when running standalone: `grep -v '^#' README.md | grep -iEc '\b(phase|task|story|PRD|cycle|milestone)\b'` returns 0; `grep -v '^#' README.md | grep -iEc '\bgsd-[a-z]'` returns 0. (Note: comment-stripping `grep -v '^#'` is not perfect for markdown — markdown headings start with #. Use the check-vocab.sh script for canonical verification.)
    - Pointer-only Contributing section (no duplication): the body between `^## Contributing` and the next `^## ` is <=5 lines.
    - License section terse: the body between `^## License` and EOF is <=5 lines.
    - When all 4 tasks land: `bash scripts/check-version-drift.sh` exits 0. (If Task 3 has not run yet, this assertion is deferred to integration verify.)
  </acceptance_criteria>
  <done>README.md is rewritten to the v2 9-section skeleton, <=500 lines, contains locked tagline + arrow chain + helpers line, 11 skills enumerated, passes vocab gate, no hardcoded version literals.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author CHANGELOG.md v2.0.0 dated entry + compress v1.x history</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (CURRENT 230 lines — read FULLY; identify the existing `## [Unreleased] — milestone v2.0.0 in progress` block and its bullets)
    - .planning/phases/01-foundation-safety-hardening/ (any 01-VERIFICATION.md or 01-*-SUMMARY.md present — extract Foundation milestone bullets)
    - .planning/phases/02-agent-layer-modernization/ (02-VERIFICATION.md / SUMMARYs — Agent milestone bullets)
    - .planning/phases/03-hook-layer-expansion/ (03-VERIFICATION.md / SUMMARYs — Hook milestone bullets)
    - .planning/phases/04-skill-layer-state-management/ (04-VERIFICATION.md / SUMMARYs — Skill milestone bullets)
    - .planning/REQUIREMENTS.md (the 46 v1 reqs + 7 QUAL reqs — ensure CHANGELOG covers them)
    - .planning/phases/05-quality-ci-tests-docs-parity/05-CONTEXT.md (D-22, D-23, D-24)
    - scripts/check-version-drift.sh (lines 58-70 — confirm CHANGELOG heading regex `^## v[0-9]+\.[0-9]+\.[0-9]+`)
  </read_first>
  <action>
Transform CHANGELOG.md to a dated v2.0.0 release entry per D-22..D-24.

Current state (CHANGELOG.md exists, 230 lines, Keep-a-Changelog format already in use, has `## [Unreleased] — milestone v2.0.0 in progress` block with bullets accumulated across Phases 1-4).

Concrete transformation steps:

STEP 1 — Preserve the file preamble (lines 1-7):

```
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/).
```

STEP 2 — Replace the `## [Unreleased] — milestone v2.0.0 in progress` heading with a dated v2.0.0 heading.

Per D-23, the heading format is `## [v2.0.0] — 2026-04-DD` (concrete date filled in this commit). However, scripts/check-version-drift.sh line 60 uses regex `^## v[0-9]+\.[0-9]+\.[0-9]+` (note: `^## v`, NO `[`). The Keep-a-Changelog `## [v2.0.0]` form would NOT match.

Decision (D-23 + version-drift contract): use the heading `## v2.0.0 — 2026-04-28` (no brackets, em-dash + ISO date). This satisfies BOTH the version-drift gate AND Keep-a-Changelog v1.1.0's "the version is the heading" rule. (Keep-a-Changelog v1.1.0 examples use `## [1.0.0] - 2017-06-20` with brackets, but the spec says "the version is the heading" — bracket vs no-bracket is style. The version-drift gate forces no-bracket.)

Actual heading (use this string verbatim, but with today's date):

  ## v2.0.0 — 2026-04-28

The date `2026-04-28` is today (CURRENT_DATE per the system context). If the executor runs this on a different date, use the actual current date in YYYY-MM-DD form.

STEP 3 — Add a release-summary paragraph immediately under the v2.0.0 heading (D-23):

```
## v2.0.0 — 2026-04-28

Polish mature version. Replaces the v1.x pipeline with a single arrow chain (/godmode → /mission → /brief → /plan → /build → /verify → /ship), hardens every defect the v1.x audit surfaced, modernizes the agent layer to the 2026 capability surface (Opus 4.7, effort: xhigh, auto mode, plugin marketplace, native skills/agents/hooks), and adds mechanical quality gates (5 CI lints, bats matrix on macOS+Linux). 46 v1 requirements + 7 QUAL requirements landed across 5 milestone areas.
```

STEP 4 — Reorganize bullets under Keep-a-Changelog sub-headings (D-22). Use this structure verbatim — the executor may add additional bullets if SUMMARY.md sources reveal more:

```
### Added

#### Foundation (substrate hardening)
- scripts/check-version-drift.sh — CI guard ensuring the version SoT (.claude-plugin/plugin.json:.version) matches every user-facing mention. (FOUND-02)
- config/quality-gates.txt — single source of truth for the 6 quality gates. (FOUND-07)
- .shellcheckrc — repo-level shellcheck configuration. (FOUND-08)
- Backup rotation in ~/.claude/backups/godmode-<ts>/ keeping last 5. (FOUND-10)
- Live filesystem indexing substrate consumed by PostCompact hook. (FOUND-11)

#### Agents (modernization)
- @planner — Opus, xhigh, read-mostly, produces PLAN.md. (AGENT-03)
- @verifier — Opus, xhigh, read-only, produces COVERED/PARTIAL/MISSING. (AGENT-04)
- @spec-reviewer (pre-execution) and @code-reviewer (post-execution) — Sonnet, high. (AGENT-05)
- scripts/check-frontmatter.sh — pure-bash + jq linter. (AGENT-06)

#### Hooks (mechanical quality gates)
- hooks/pre-tool-use.sh — blocks `--no-verify`, secret pattern scan. (HOOK-01, HOOK-02)
- hooks/post-tool-use.sh — surfaces failed gate exit codes in next turn. (HOOK-03)
- hooks/session-start.sh STATE injection. (HOOK-04)
- hooks/post-compact.sh live FS scan + gates from config. (HOOK-05)

#### Skills (workflow rebuild)
- 11 user-facing skills: /godmode, /mission, /brief, /plan, /build, /verify, /ship, /debug, /tdd, /refactor, /explore-repo. (WORKFLOW-01..WORKFLOW-09)
- Auto Mode detection in every workflow skill. (WORKFLOW-11)
- skills/_shared/init-context.sh — pure-bash + jq state helper. (WORKFLOW-12)
- .planning/ artifact templates under templates/.planning/. (WORKFLOW-13)

#### Quality (CI, tests, docs)
- .github/workflows/ci.yml — 5 lint gates + bats matrix on macOS+Linux. (QUAL-01)
- tests/install.bats — install round-trip + adversarial-branch fixtures + settings-merge regression. (QUAL-02, QUAL-07)
- scripts/check-parity.sh — plugin/manual hooks parity gate. (QUAL-03)
- scripts/check-vocab.sh — vocabulary + surface-count gate. (QUAL-04)

### Changed

- install.sh — version sourced from plugin.json:.version; per-file diff/skip/replace prompt; non-TTY default keeps customizations. (FOUND-01, FOUND-02)
- uninstall.sh — refuses on version mismatch unless --force. (FOUND-03)
- config/statusline.sh — collapsed 4 jq calls into 1 via @tsv. (FOUND-06)
- 8 v1.x agents migrated to v2 frontmatter convention (model alias, effort tier, Connects-to). (AGENT-07)
- .claude-plugin/plugin.json description + keywords polished for marketplace SEO. (QUAL-06)
- README.md rewritten to v2 9-section skeleton (<=500 lines, tutorial-first). (QUAL-05)

### Fixed

- Hook JSON construction now uses jq -n --arg; valid JSON under adversarial branch names (quote, backslash, newline, apostrophe). (FOUND-04 / CONCERNS #6)
- Hook cwd resolution from stdin envelope (not bare pwd); stdin-drain tolerance with `cat > /dev/null || true`. (FOUND-05 / CONCERNS #7, #18)
- Settings merge no longer drops top-level user keys not in template. (QUAL-07 / CONCERNS #3)

### Removed

- v1.x /prd, /plan-stories, /execute skills are deprecated with migration banners; will be removed in v2.x. (WORKFLOW-10)
- v1.x version literals in install.sh, commands/godmode.md, README. (FOUND-02)

### Security

- PreToolUse hook blocks `git commit --no-verify`, `git push --force` to main, and hardcoded secret patterns (AWS keys, GitHub PATs, JWT shapes). (HOOK-01, HOOK-02)
```

STEP 5 — Compress v1.x history (D-24). Remove all individual `## [v1.5.x]`, `## [v1.4.x]`, `## [v1.3.x]`, `## [v1.6.0]` etc. blocks from the current CHANGELOG. Replace with a single block at the bottom:

```
## v1.x.x

v1.x history compressed. The v1.x pipeline was /prd -> /plan-stories -> /execute -> /ship with 8 agents and 8 skills. v2.0.0 supersedes the entire surface; v1.x deprecation banners on /prd, /plan-stories, /execute guide migration through v2.x. For v1.x release-by-release detail, see git history before tag v2.0.0.
```

STEP 6 — Save and verify locally:
- `head -1 CHANGELOG.md` is `# Changelog`.
- `grep -nE '^## v[0-9]+\.[0-9]+\.[0-9]+' CHANGELOG.md | head -1` returns line with `## v2.0.0 — 2026-04-28`.
- `bash scripts/check-version-drift.sh` exits 0 (depends on Task 3 setting plugin.json:.version=2.0.0; if Task 3 has not yet run, document deferred verification in 05-03-SUMMARY.md).
- `wc -l CHANGELOG.md` returns a sensible count (likely 100-200 lines after compression).
  </action>
  <verify>
    <automated>head -1 CHANGELOG.md | grep -q '^# Changelog' && grep -qE '^## v2\.0\.0 ' CHANGELOG.md && grep -qE '^### Added' CHANGELOG.md && grep -qE '^### Changed' CHANGELOG.md && grep -qE '^### Fixed' CHANGELOG.md && grep -qE '^### Removed' CHANGELOG.md && grep -qE '^### Security' CHANGELOG.md && grep -qE '^## v1\.x\.x' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and starts with `# Changelog`: `head -1 CHANGELOG.md` returns `# Changelog`.
    - Preamble preserved: `head -7 CHANGELOG.md | grep -c 'Keep a Changelog'` returns 1.
    - Top version heading matches drift-gate regex: `grep -nE '^## v[0-9]+\.[0-9]+\.[0-9]+' CHANGELOG.md | head -1` returns line containing `## v2.0.0`.
    - Dated heading: `grep -cE '^## v2\.0\.0 — 20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]' CHANGELOG.md` returns 1.
    - All 5 Keep-a-Changelog sub-headings present: each of `^### Added`, `^### Changed`, `^### Fixed`, `^### Removed`, `^### Security` matched once via grep.
    - Milestone-area H4 sub-blocks under Added: each of `^#### Foundation`, `^#### Agents`, `^#### Hooks`, `^#### Skills`, `^#### Quality` returns 1 hit.
    - Requirement IDs cited: `grep -cE '\(FOUND-[0-9]+\)|\(AGENT-[0-9]+\)|\(HOOK-[0-9]+\)|\(WORKFLOW-[0-9]+\)|\(QUAL-[0-9]+\)' CHANGELOG.md` returns >=20 (rough lower bound — at least 1 per major bullet group).
    - v1.x compressed: `grep -cE '^## v1\.x\.x' CHANGELOG.md` returns 1; `grep -cE '^## v1\.[0-9]+\.[0-9]+' CHANGELOG.md` returns 0 (no individual v1.x.y headings remain).
    - No leftover `## [Unreleased]` block: `grep -c '## \[Unreleased\]' CHANGELOG.md` returns 0.
    - When Task 3 has set plugin.json:.version=2.0.0: `bash scripts/check-version-drift.sh` exits 0.
    - Reasonable file size: `wc -l CHANGELOG.md` returns a value between 80 and 250.
  </acceptance_criteria>
  <done>CHANGELOG.md has dated v2.0.0 heading matching version-drift regex, full Keep-a-Changelog taxonomy with 5 sub-headings + milestone-area H4s, requirement IDs cited, v1.x history compressed to a single block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Polish .claude-plugin/plugin.json (description, keywords, version 2.0.0; preserve userConfig)</name>
  <files>.claude-plugin/plugin.json</files>
  <read_first>
    - .claude-plugin/plugin.json (CURRENT 26 lines — read FULLY; verify all current keys; note current `version` is `1.6.0` and current `description`+`keywords` are v1.x-shaped)
    - .planning/phases/05-quality-ci-tests-docs-parity/05-CONTEXT.md (D-25, D-26)
    - .planning/phases/05-quality-ci-tests-docs-parity/05-PATTERNS.md `.claude-plugin/plugin.json` section
    - .planning/phases/01-foundation-safety-hardening/ (look for evidence that userConfig.model_profile was added in Phase 1; if not present, that is a Phase 1 gap and this plan should NOT add it — only PRESERVE if present)
    - .planning/research/STACK.md "Plugin manifest" section (userConfig schema)
    - scripts/check-version-drift.sh (read full — confirms canonical version drives all other mentions)
  </read_first>
  <action>
Polish .claude-plugin/plugin.json with three changes (D-25, D-26):

CHANGE 1 — `description`: replace v1.x string with locked v2 string. The locked string (D-25, 197 chars, under 200-char marketplace cap) is:

  Senior engineering team, in a plugin. One arrow chain (/godmode → /mission → /brief → /plan → /build → /verify → /ship), 11 skills, mechanical quality gates.

The arrow character is U+2192 (Unicode rightwards arrow). Type the actual character into the JSON — do NOT use ASCII `->`.

CHANGE 2 — `keywords`: replace v1.x array with v2 array (D-25):

  ["workflow","agents","skills","hooks","planning","quality-gates","auto-mode","claude-code"]

This REMOVES `pipeline`, `engineering`, `code-review`, `tdd`, `refactoring`, `prd` (all v1.x-shaped or duplicative). ADDS `planning`, `auto-mode`. The `prd` removal is also vocab discipline — `PRD` is forbidden in user-facing surfaces, and plugin.json is user-facing (it appears in marketplace listing).

CHANGE 3 — `version`: bump from `1.6.0` to `2.0.0`.

PRESERVE all other top-level keys: `name`, `author`, `homepage`, `repository`, `license`. AND if `userConfig` (with `model_profile`) was added by Phase 1, PRESERVE it untouched (D-26 — "Don't strip it; it's not metadata.").

CRITICAL: do NOT strip the userConfig block if it exists in the current file. The current file (read above) shows what is present — if userConfig is absent in the current file, that is a Phase 1 gap that is OUT OF SCOPE for this plan to fix.

Implementation approach:

Option A (preferred — preserves key ordering and trailing-newline semantics): use jq:

```bash
jq '
  .description = "Senior engineering team, in a plugin. One arrow chain (/godmode → /mission → /brief → /plan → /build → /verify → /ship), 11 skills, mechanical quality gates."
  | .version = "2.0.0"
  | .keywords = ["workflow","agents","skills","hooks","planning","quality-gates","auto-mode","claude-code"]
' .claude-plugin/plugin.json > .claude-plugin/plugin.json.tmp \
  && mv .claude-plugin/plugin.json.tmp .claude-plugin/plugin.json
```

Note: jq emits the U+2192 character correctly when given a UTF-8 input string; the shell wrapping just needs to handle the literal character. If the executor's shell has trouble with the inline arrow character, use jq's --arg to pass the description as a parameter:

```bash
DESC="Senior engineering team, in a plugin. One arrow chain (/godmode → /mission → /brief → /plan → /build → /verify → /ship), 11 skills, mechanical quality gates."
jq --arg desc "$DESC" \
  '.description = $desc | .version = "2.0.0" | .keywords = ["workflow","agents","skills","hooks","planning","quality-gates","auto-mode","claude-code"]' \
  .claude-plugin/plugin.json > .claude-plugin/plugin.json.tmp \
  && mv .claude-plugin/plugin.json.tmp .claude-plugin/plugin.json
```

This jq pipeline preserves all other top-level keys (name, author, homepage, repository, license, AND userConfig if present) — jq's `.field = value` operator only mutates the named fields; everything else passes through.

Option B (Edit tool): make 3 separate Edit operations on the file. Acceptable but less robust to whitespace drift than jq. If using Edit, verify the final file with `jq -e '.' .claude-plugin/plugin.json` to ensure JSON validity.

Verification (post-write):

```bash
# 1. JSON validity
jq -e '.' .claude-plugin/plugin.json >/dev/null

# 2. Description length <= 200 chars
DESC_LEN=$(jq -r '.description | length' .claude-plugin/plugin.json)
test "$DESC_LEN" -le 200

# 3. Description starts with locked tagline
jq -r '.description' .claude-plugin/plugin.json | head -c 38 | grep -q '^Senior engineering team, in a plugin.'

# 4. Version is 2.0.0
test "$(jq -r '.version' .claude-plugin/plugin.json)" = "2.0.0"

# 5. Keywords match v2 set (length and content)
test "$(jq -r '.keywords | length' .claude-plugin/plugin.json)" = "8"
jq -r '.keywords[]' .claude-plugin/plugin.json | sort | tr '\n' ' ' | grep -q 'agents auto-mode claude-code hooks planning quality-gates skills workflow '

# 6. userConfig preserved if previously present (don't fail if absent — that's a Phase 1 gap)
# (manual check — read the file post-write)

# 7. License preserved
test "$(jq -r '.license' .claude-plugin/plugin.json)" = "MIT"

# 8. Author / homepage / repository preserved
jq -e '.author and .homepage and .repository' .claude-plugin/plugin.json >/dev/null
```

After this task lands, `bash scripts/check-version-drift.sh` SHOULD exit 0 (the canonical version is now `2.0.0`, matching the CHANGELOG heading from Task 2 and any README references — assuming Task 1 stripped all hardcoded version literals from README).
  </action>
  <verify>
    <automated>jq -e '.' .claude-plugin/plugin.json >/dev/null && test "$(jq -r '.version' .claude-plugin/plugin.json)" = "2.0.0" && test "$(jq -r '.description | length' .claude-plugin/plugin.json)" -le 200 && test "$(jq -r '.keywords | length' .claude-plugin/plugin.json)" = "8" && test "$(jq -r '.license' .claude-plugin/plugin.json)" = "MIT"</automated>
  </verify>
  <acceptance_criteria>
    - Valid JSON: `jq -e '.' .claude-plugin/plugin.json >/dev/null` exits 0.
    - Version is `2.0.0`: `jq -r '.version' .claude-plugin/plugin.json` returns `2.0.0`.
    - Description starts with locked tagline: `jq -r '.description' .claude-plugin/plugin.json | head -c 38` returns `Senior engineering team, in a plugin.`.
    - Description <= 200 chars: `jq -r '.description | length' .claude-plugin/plugin.json` returns a value <=200.
    - Description contains arrow chain: `jq -r '.description' .claude-plugin/plugin.json | grep -c '/godmode .* /mission'` returns 1.
    - Keywords are exactly the v2 set: `jq -r '.keywords | sort | join(",")' .claude-plugin/plugin.json` returns `agents,auto-mode,claude-code,hooks,planning,quality-gates,skills,workflow`.
    - Keywords array length = 8: `jq -r '.keywords | length' .claude-plugin/plugin.json` returns `8`.
    - License preserved: `jq -r '.license' .claude-plugin/plugin.json` returns `MIT`.
    - author/homepage/repository preserved: `jq -e '.author and .homepage and .repository' .claude-plugin/plugin.json` exits 0.
    - userConfig preserved if present in pre-state: if Phase 1 added userConfig.model_profile, post-write `jq -r '.userConfig.model_profile' .claude-plugin/plugin.json` returns its previous value (likely `balanced`). If userConfig was NOT present in pre-state, this assertion is N/A and recorded in 05-03-SUMMARY.md as a Phase 1 gap (OUT OF SCOPE for this plan).
    - No removed v1.x keywords linger: `jq -r '.keywords | join(",")' .claude-plugin/plugin.json | grep -cE '\b(pipeline|engineering|code-review|tdd|refactoring|prd)\b'` returns 0.
    - When Tasks 1+2 also land: `bash scripts/check-version-drift.sh` exits 0.
  </acceptance_criteria>
  <done>plugin.json has version 2.0.0, locked v2 description (≤200 chars, contains tagline + arrow chain), v2 keywords (exactly 8 specified), all other top-level keys preserved, valid JSON.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Light-touch CONTRIBUTING.md (README pointer + tag-protection note)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (CURRENT 133 lines — read fully; identify where the pointer goes (top of file, after H1) and where the tag-protection note goes (under or near `## PR Process` section))
    - .planning/phases/05-quality-ci-tests-docs-parity/05-CONTEXT.md (D-29)
    - .planning/phases/05-quality-ci-tests-docs-parity/05-PATTERNS.md CONTRIBUTING.md section
  </read_first>
  <action>
Two minimal insertions into CONTRIBUTING.md (D-29). Do NOT rewrite the file. The existing content (133 lines, v1.x-shaped) stays largely as-is — Phase 5 scope explicitly says CONTRIBUTING is internal-docs and the vocab gate exempts it.

INSERTION 1 — Top-of-file pointer block, immediately after the H1 heading:

```markdown
> For installation and usage, see [README.md](README.md). This file is the
> developer manual: how to add agents/skills/hooks/rules, run CI locally,
> and propose changes.
```

This closes the "no-duplication" rule of D-21 — README is marketing front door, this is dev manual.

INSERTION 2 — Tag-protection note. Find the `## PR Process` section (or the section closest to PR/release content; if no `## PR Process` section exists, append the new H3 block at the END of the file under the most-relevant existing section, OR add a new H2 section `## Release` with this single H3 underneath).

The exact text to insert (as an H3 under PR Process or as standalone block):

```markdown
### Tag protection (release process)

`v*` tags trigger marketplace re-indexing. Repository admins enable tag
protection in GitHub settings (Settings → Tags → New rule → `v*`).
Non-admin pushes of `v*` tags are rejected at the GitHub-API level. v2.0
relies on this UI setting; v2.x may add mechanical enforcement.
```

Implementation:

1. Use the Edit tool (or Read + targeted Write) to make exactly these 2 insertions. Do NOT touch any other lines.

2. After the insertions, run `wc -l CONTRIBUTING.md` — expect ~145-150 lines (133 original + ~12-15 inserted).

3. Do NOT modify:
   - Lines 5-46 (Contribution Paths for agents/skills/rules/hooks) — Phase 4-shaped already; updating to v2 vocabulary is OUT OF SCOPE for Phase 5 (CONTRIBUTING is internal-docs).
   - Line 57 "File Structure (v1.4)" — stale heading, but inside the file. OK to fix in passing if planner wants, but not required.
   - Existing `## PR Process`, `## Code Style`, etc. sections.

What this plan deliberately does NOT do:
- Rewrite the dev manual to v2 shape (deferred to v2.x).
- Update line 57 stale heading (out of scope per CONTEXT § Out of scope, OUT-05).
- Document the new lint scripts (check-parity.sh, check-vocab.sh) — that is documentation drift; if useful, can be added in a separate v2.x docs polish pass.
  </action>
  <verify>
    <automated>grep -qF 'For installation and usage, see [README.md]' CONTRIBUTING.md && grep -qE '^### Tag protection' CONTRIBUTING.md && grep -qF 'v*' CONTRIBUTING.md && test "$(wc -l < CONTRIBUTING.md | tr -d ' ')" -ge 130</automated>
  </verify>
  <acceptance_criteria>
    - File still exists and has not been deleted: `[ -f CONTRIBUTING.md ]`.
    - Top pointer present: `grep -cF 'For installation and usage, see [README.md](README.md)' CONTRIBUTING.md` returns 1.
    - Tag-protection note present: `grep -cE '^### Tag protection' CONTRIBUTING.md` returns 1.
    - Tag-protection note mentions `v*` and `Settings → Tags`: `grep -cF 'Settings → Tags' CONTRIBUTING.md` returns 1.
    - Insertions did not delete existing content: `wc -l CONTRIBUTING.md | tr -d ' '` returns >=130 (original 133 + insertions, possibly minus 1-2 if old content was reflowed).
    - File is still valid markdown (no broken structure): `head -1 CONTRIBUTING.md` returns the existing H1 heading.
    - No new forbidden vocabulary added by the insertions: the Tag protection block does not use phase/task/story/PRD/cycle/milestone (CONTRIBUTING is exempt from vocab gate but stylistically should match).
  </acceptance_criteria>
  <done>CONTRIBUTING.md has top-of-file README pointer block + tag-protection H3 note. No other content modified. File line count ≥130.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marketplace rendering -> plugin.json | Marketplace reads plugin.json description + keywords for listing; trusted source (this repo). |
| README/CHANGELOG -> users | Users read these for install instructions; misleading content is a trust hazard. |
| Tag-protection note in CONTRIBUTING.md -> repo admin | Note documents UI-side enforcement; does not enforce mechanically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Spoofing | Marketplace listing — fake metadata claiming v1.x state | mitigate | Description ≤200 chars + locked v2 string makes claims explicit and verifiable; CI version-drift gate (Plan 05-01) catches mismatches. |
| T-05-13 | Tampering | README install instructions point at malicious URL | accept | README is committed via PR review; install URL points at this repo's GitHub Releases. Hijack-attempts surface in PR diff review. |
| T-05-14 | Information Disclosure | CHANGELOG references private security details | mitigate | Security section bullets describe behavior, not exploit details. PreToolUse hook patterns are public (already in hooks/pre-tool-use.sh source). |
| T-05-15 | Tampering | userConfig.model_profile silently stripped during marketplace polish | mitigate | Task 3 acceptance criteria explicitly verify userConfig preservation if present in pre-state. jq pipeline only mutates named fields, leaving everything else untouched. |
| T-05-16 | Elevation of Privilege | Tag-protection bypass (non-admin pushes v*) | accept | UI-only enforcement per D-29; mechanical enforcement deferred to v2.x. Documented limitation. |
| T-05-17 | Repudiation | CHANGELOG date drift between commit and tag | accept | The date in `## v2.0.0 — YYYY-MM-DD` is the merge date, not the tag date. If they differ by 1 day, that is acceptable. v2.0.1+ may correct via amend or new entry. |

</threat_model>

<verification>
- README.md exists, ≤500 lines, has 9 H2 sections per D-19, contains locked tagline and arrow chain, passes `bash scripts/check-vocab.sh`.
- CHANGELOG.md has dated `## v2.0.0 —` heading at top (after preamble), Keep-a-Changelog 5-section taxonomy, requirement IDs cited, v1.x compressed.
- .claude-plugin/plugin.json has version 2.0.0, locked v2 description (≤200 chars), v2 keywords array (length 8), userConfig preserved if previously present, valid JSON.
- CONTRIBUTING.md has README pointer + tag-protection H3 block, otherwise unmodified (133 → ~145 lines).
- After all 4 tasks: `bash scripts/check-version-drift.sh` exits 0 (depends on Plan 05-01's check-version-drift.sh — pre-existing Phase 1 deliverable).
- After all 4 tasks: `bash scripts/check-vocab.sh` exits 0 (depends on Plan 05-01's new check-vocab.sh).
</verification>

<success_criteria>
- 4 MODIFIED files committed: README.md, CHANGELOG.md, .claude-plugin/plugin.json, CONTRIBUTING.md.
- README.md is the v2 marketing front door — ≤500 lines, 9-section skeleton, tutorial-first, no v1.x vocabulary.
- CHANGELOG.md v2.0.0 dated entry summarizes 5 milestone areas in Keep-a-Changelog format.
- plugin.json marketplace polish complete (description, keywords, version 2.0.0); userConfig preserved.
- CONTRIBUTING.md has README pointer (no duplication) + tag-protection note.
- All version mentions across files coherent — `bash scripts/check-version-drift.sh` exits 0.
- README content passes vocab gate.
- The v2.0.0 git tag is documented as a follow-up step in 05-03-SUMMARY.md (D-27 — tag from main AFTER PR merges and CI is green; this plan does NOT cut the tag).
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-ci-tests-docs-parity/05-03-SUMMARY.md` documenting:
- The 4 files modified (paths + line counts before/after).
- Sample diff of plugin.json (key fields: description before/after, keywords before/after, version before/after).
- Hand-off note: the v2.0.0 git tag is the post-merge step. Per D-27, after the Phase 5 PR merges to main and CI is green, run:
    git checkout main && git pull && git tag v2.0.0 && git push --tags
- Verify .claude-plugin/plugin.json:.version matches the tag.
- Note any deviations from D-18..D-29 with rationale.
- If userConfig.model_profile was NOT present in plugin.json before this plan, flag as a Phase 1 gap.
</output>
