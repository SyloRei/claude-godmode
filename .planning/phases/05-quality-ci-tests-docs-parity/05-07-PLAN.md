---
phase: 05-quality-ci-tests-docs-parity
plan: 07
type: execute
wave: 2
depends_on: ["05-04"]
gap_ids: ["CR-04"]
requirements: []
files_modified:
  - CONTRIBUTING.md
autonomous: true
goal: "Rewrite CONTRIBUTING.md to match v2 reality: replace v1.4 file-structure block with v2.0 layout, replace `effort: max` references with the correct `effort: high|xhigh` policy plus the Opus-4.7 xhigh rules-skipping pitfall, enumerate all 12 v2 agents with correct (model, effort) tiers, and point skill-conventions readers at `rules/godmode-skills.md` instead of `rules/godmode-routing.md`."

must_haves:
  truths:
    - "`grep -F 'File Structure (v1.4)' CONTRIBUTING.md` returns no matches"
    - "`grep -F 'File Structure (v2.0)' CONTRIBUTING.md` returns at least 1 match"
    - "`grep -F 'effort: max' CONTRIBUTING.md` returns no matches"
    - "`grep -cE 'effort: (high|xhigh)' CONTRIBUTING.md` returns at least 6 matches"
    - "`grep -cE '@(architect|security-auditor|planner|verifier|executor|writer|test-writer|reviewer|spec-reviewer|code-reviewer|researcher|doc-writer)' CONTRIBUTING.md` returns at least 12 matches (every v2 agent named at least once)"
    - "`grep -F 'rules/godmode-skills.md' CONTRIBUTING.md` returns at least 1 match"
    - "`grep -F 'xhigh skips rules on Opus 4.7' CONTRIBUTING.md` returns at least 1 match"
    - "`grep -cF 'For installation and usage, see [README.md](README.md)' CONTRIBUTING.md` returns 1 (Plan 05-03 README pointer preserved)"
    - "`grep -cE '^### Tag protection' CONTRIBUTING.md` returns 1 (Plan 05-03 Tag protection H3 preserved)"
    - "`bash scripts/check-vocab.sh` exits 0 (CONTRIBUTING is internal-docs vocab-exempt; we don't introduce a regression)"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "v2 dev manual: accurate file structure, 12-agent enumeration with model+effort tiers, correct skill-conventions pointer, Opus-4.7 xhigh pitfall, README pointer + Tag protection retained"
      contains: "File Structure (v2.0)"
      contains_extra:
        - ".claude-plugin/"
        - "scripts/"
        - "tests/"
        - ".github/workflows/"
        - "config/quality-gates.txt"
        - "templates/"
        - "bin/"
        - "rules/godmode-skills.md"
        - "@architect"
        - "@security-auditor"
        - "@planner"
        - "@verifier"
        - "@executor"
        - "@writer"
        - "@test-writer"
        - "@reviewer"
        - "@spec-reviewer"
        - "@code-reviewer"
        - "@researcher"
        - "@doc-writer"
        - "effort: high"
        - "effort: xhigh"
        - "xhigh skips rules on Opus 4.7"
  key_links:
    - from: "CONTRIBUTING.md § Model Selection"
      to: "agents/*.md frontmatter (model + effort fields)"
      via: "documentation describing the actual frontmatter contract"
      status: "post-plan: doc matches the 12 agent files' real (model, effort) tiers"
    - from: "CONTRIBUTING.md § File Structure (v2.0)"
      to: "actual repo layout (.claude-plugin/, scripts/, tests/, .github/, config/, templates/, bin/)"
      via: "tree block enumerating real directories"
      status: "post-plan: contributors see directories that exist"
    - from: "CONTRIBUTING.md § New Skill"
      to: "rules/godmode-skills.md"
      via: "pointer to skill-frontmatter conventions"
      status: "post-plan: pointed at the right rule file"
---

<objective>
Close CR-04 from `05-VERIFICATION.md`. CONTRIBUTING.md's "light-touch" update
under Plan 05-03 (D-29) added the README pointer + tag-protection note (which
DID land — lines 3-5, 126-131). The rest of the file is materially out of date
for v2.0:

- Line 61: `## File Structure (v1.4)` heading. Tree block omits `.claude-plugin/`,
  `scripts/`, `tests/`, `.github/workflows/`, `config/quality-gates.txt`,
  `templates/`, `bin/`. A new contributor following CONTRIBUTING today does not
  know about the CI gates and will not run them locally.
- Line 106: references `effort: max` — not a valid project value. v2 uses
  `high` (code-writing) and `xhigh` (audit/design). Worse, line 106 says `max`
  is "Opus-exclusive". The real Opus-4.7 pitfall is the OPPOSITE: `xhigh` skips
  rules on Opus 4.7 and must NOT be used on code-writing agents.
- Lines 86-106: list only 8 v1.x agents. The 4 v2-shipped agents (`@planner`,
  `@verifier`, `@spec-reviewer`, `@code-reviewer`) are invisible.
- Lines 24/30: New Skill / New Agent point at `rules/godmode-routing.md` for
  skill conventions. Skill conventions actually live in `rules/godmode-skills.md`.

This plan rewrites CONTRIBUTING.md as a v2 dev manual — preserving the v2
anchors (README pointer at top, Tag protection H3 near PR Process) and the
existing Bug Reports / Questions sections, but rewriting File Structure, Model
Selection, and rule-file pointers to match reality.

Sequencing: depends_on ["05-04"]. CONTRIBUTING.md has no `phase`/`milestone`
violations on the current tree (it is internal-doc, exempt per D-12), but the
rewrite references skill conventions that 05-04 finalizes. Sequencing 05-07
after 05-04 ensures "see also" pointers describe the post-scrub state.

Purpose: a new contributor following CONTRIBUTING today produces frontmatter
consistent with the rest of `agents/`, knows about the CI gates and how to run
them locally, and is pointed at the correct rule file for skill conventions.
Output: 1 file modification (`CONTRIBUTING.md`); approximately a 50-line delta
concentrated in File Structure, Model Selection, and skill/agent pointers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-quality-ci-tests-docs-parity/05-CONTEXT.md
@.planning/phases/05-quality-ci-tests-docs-parity/05-VERIFICATION.md
@CONTRIBUTING.md
@CLAUDE.md
@README.md

<interfaces>
Real (model, effort) tiers from agents/*.md frontmatter — verified live:

opus + xhigh: @architect, @planner, @security-auditor, @verifier
opus + high:  @executor, @writer
sonnet + high: @code-reviewer, @doc-writer, @researcher, @reviewer, @spec-reviewer, @test-writer

12 agents total. No agent uses `effort: max` (that value does not exist in v2).
The pitfall: `xhigh skips rules on Opus 4.7` per PROJECT.md Key Decisions and
CLAUDE.md "Default model assignments". This is why @writer and @executor use
`effort: high` even though they run on Opus 4.7.

Real v2 directory layout (verified via `ls`): .claude-plugin/, .github/workflows/,
agents/, bin/, commands/, config/, hooks/, rules/, scripts/, skills/, templates/,
tests/. Plus root files: CHANGELOG.md, CLAUDE.md, CONTRIBUTING.md, README.md,
install.sh, uninstall.sh.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the New Agent / New Skill / Adding Rules / New Hook subsections (lines 9-59 region)</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (full file — current 144 lines)
    - rules/godmode-skills.md (confirm it covers skill frontmatter conventions)
    - rules/godmode-routing.md (confirm it covers agent routing, NOT skill frontmatter)
  </read_first>
  <action>
Edit CONTRIBUTING.md. Replace the New Agent block and the New Skill block.

**Edit 1 — New Agent block.**

old_string (verbatim from current file):
```
### New Agent

1. Create `agents/<name>.md` with a system prompt
2. Define the agent's model, permissions, and purpose in the frontmatter
3. Add `memory:` to the frontmatter -- choose the appropriate scope:
   - **project** -- the default for most agents. Codebase patterns, conventions, and findings are project-specific and useful for the whole team. Used by all current godmode agents.
   - **user** -- cross-project learnings. Use only when findings genuinely benefit the user regardless of which project they're in. Currently unused in godmode (all agents use project).
   - **local** -- sensitive or private findings. Use when memory may contain credentials, vulnerability details, or other data that should not be committed. Currently unused in godmode.
4. Consider additional frontmatter fields:
   - `effort: high` -- for agents where thoroughness is critical (reviewers, security, architecture)
   - `maxTurns: N` -- safety valve for agents that write code (prevents runaway token burn)
   - `disallowedTools: Write, Edit` -- enforce read-only mechanically on read-only agents
   - `background: true` -- for agents typically spawned for non-blocking parallel work
5. Add a routing entry in `rules/godmode-routing.md` under "When to Use What"
```

new_string:
```
### New Agent

1. Create `agents/<name>.md` with a system prompt and YAML frontmatter
2. Set `model:` and `effort:` per the four-tier policy below (see Model Selection)
3. Add `memory:` to the frontmatter -- choose the appropriate scope:
   - **project** -- the default for most agents. Codebase patterns, conventions, and findings are project-specific and useful for the whole team. Used by all current godmode agents.
   - **user** -- cross-project learnings. Use only when findings genuinely benefit the user regardless of which project they're in. Currently unused in godmode.
   - **local** -- sensitive or private findings. Use when memory may contain credentials, vulnerability details, or other data that should not be committed. Currently unused in godmode.
4. Consider additional frontmatter fields:
   - `maxTurns: N` -- safety valve for agents that write code (prevents runaway token burn)
   - `disallowedTools: Write, Edit` -- enforce read-only mechanically on read-only agents
   - `background: true` -- for agents typically spawned for non-blocking parallel work
   - `isolation: worktree` -- required on every code-writing agent (`@executor`, `@writer`, `@test-writer`)
5. Add a routing entry in `rules/godmode-routing.md` under "When to Use What"
6. Run `bash scripts/check-frontmatter.sh` locally to confirm the linter is clean
```

**Edit 2 — New Skill block.**

old_string (verbatim):
```
### New Skill

1. Create `skills/<name>/SKILL.md` with the skill definition
2. Include: trigger command, description, step-by-step workflow, and quality gate requirements
3. Add a routing entry in `rules/godmode-routing.md` under "When to Use What"
```

new_string:
```
### New Skill

1. Create `skills/<name>/SKILL.md` with the skill definition and YAML frontmatter
2. Include: `name:`, `description:` (≤1,536 chars combined with `when_to_use`), `argument-hint:` if the skill takes args, and `allowed-tools:` scoped to the minimum needed
3. Body: trigger command, description, step-by-step workflow, and quality gate requirements
4. Follow the conventions in `rules/godmode-skills.md` (frontmatter contract, Connects-to layout, Auto Mode detection, vocabulary discipline)
5. Run `bash scripts/check-vocab.sh` locally to confirm no forbidden vocabulary leaks into user-facing prose
6. Run `bash scripts/check-frontmatter.sh` locally to confirm the frontmatter linter is clean
```
  </action>
  <acceptance_criteria>
    - `grep -F 'rules/godmode-skills.md' CONTRIBUTING.md` matches at least 1 line
    - `grep -F 'isolation: worktree' CONTRIBUTING.md` matches at least 1 line
    - `grep -F 'bash scripts/check-vocab.sh' CONTRIBUTING.md` matches at least 1 line
    - `grep -F 'bash scripts/check-frontmatter.sh' CONTRIBUTING.md` matches at least 1 line
    - `grep -cF 'For installation and usage, see [README.md](README.md)' CONTRIBUTING.md` outputs `1` (README pointer preserved)
  </acceptance_criteria>
  <verify>
    <automated>set -e; cd /Users/sylorei/pet-projects/claude-godmode; grep -qF 'rules/godmode-skills.md' CONTRIBUTING.md &amp;&amp; grep -qF 'isolation: worktree' CONTRIBUTING.md &amp;&amp; grep -qF 'bash scripts/check-vocab.sh' CONTRIBUTING.md &amp;&amp; grep -qF 'bash scripts/check-frontmatter.sh' CONTRIBUTING.md &amp;&amp; test "$(grep -cF 'For installation and usage, see [README.md](README.md)' CONTRIBUTING.md)" = "1"</automated>
  </verify>
  <done>New Agent + New Skill sections point contributors at correct rule files and CI scripts; README pointer preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Replace File Structure (v1.4) block with v2.0 layout</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (lines 61-75 region — the v1.4 block to replace)
    - The v2 directory layout in this plan's `<interfaces>` section
  </read_first>
  <action>
Replace the entire `## File Structure (v1.4)` section with a v2.0 file structure block that enumerates the real v2 directories.

old_string (verbatim — full v1.4 block including the surrounding fence lines):
```
## File Structure (v1.4)

```
claude-godmode/
  agents/           # Agent definitions (*.md with frontmatter)
  commands/         # Slash commands (e.g., /godmode)
  config/           # Settings template and statusline
    settings.template.json
    statusline.sh
  hooks/            # Hook scripts and hooks.json
  rules/            # Rule files (godmode-*.md) -> ~/.claude/rules/
  skills/           # Skill definitions (SKILL.md per directory)
  install.sh        # Installer (plugin-mode + manual-mode)
  uninstall.sh      # Clean removal of godmode artifacts
```
```

new_string:
```
## File Structure (v2.0)

```
claude-godmode/
  .claude-plugin/        # plugin manifest (plugin.json — canonical version SoT)
  .github/workflows/     # CI (ci.yml: 5 lint gates + bats matrix on macos+ubuntu)
  agents/                # 12 v2 agents (*.md with model/effort/memory frontmatter)
  bin/                   # bare commands installed onto PATH (e.g., godmode-state)
  commands/              # /godmode entry point (single user-facing command file)
  config/                # settings template, statusline, canonical quality gates
    quality-gates.txt    #   single source of truth for the 6 commit-time gates
    settings.template.json #   merged into ~/.claude/settings.json on install
    statusline.sh        #   shipped statusline (cost + context + workflow status)
  hooks/                 # hook scripts + hooks.json (PreToolUse, PostToolUse,
                         #   SessionStart, PostCompact, UserPromptSubmit)
  rules/                 # godmode-*.md -> ~/.claude/rules/ (one concern per file)
  scripts/               # CI-invoked lint scripts (check-vocab/parity/version-drift/
                         #   frontmatter — same scripts run locally and in CI)
  skills/                # 11 v2 user-invocable skills + 3 v1.x deprecation banners
  templates/.planning/   # planning artifact templates (PROJECT.md, ROADMAP.md, etc.)
  tests/                 # bats-core suite + fixtures
    install.bats         #   install -> uninstall -> reinstall round-trip + adversarial
    fixtures/branches/   #   adversarial-branch JSON fixtures (FOUND-04 regression)
  CHANGELOG.md           # Keep-a-Changelog format; canonical version source: plugin.json
  CLAUDE.md              # repo conventions; loaded into every Claude Code session
  CONTRIBUTING.md        # this file
  README.md              # marketing front door (≤500 lines, vocab-clean)
  install.sh             # installer (plugin-mode + manual-mode parity)
  uninstall.sh           # clean removal; refuses on version mismatch (FOUND-03)
```

Run any of these locally before opening a PR to mirror the CI gates:

```bash
bash scripts/check-version-drift.sh   # version SoT (.claude-plugin/plugin.json:.version)
bash scripts/check-frontmatter.sh     # agent + skill YAML frontmatter linter
bash scripts/check-parity.sh          # plugin-mode vs manual-mode hook bindings
bash scripts/check-vocab.sh           # forbidden vocab + 11-skill surface count
shellcheck $(find . -name '*.sh' -not -path './.git/*')
bats tests/install.bats               # install round-trip + adversarial fixtures
```
```
  </action>
  <acceptance_criteria>
    - `grep -F 'File Structure (v1.4)' CONTRIBUTING.md` matches no lines (exit 1)
    - `grep -F 'File Structure (v2.0)' CONTRIBUTING.md` matches at least 1 line
    - `grep -F '.claude-plugin/' CONTRIBUTING.md` matches at least 1 line
    - `grep -F '.github/workflows/' CONTRIBUTING.md` matches at least 1 line
    - `grep -F 'config/quality-gates.txt' CONTRIBUTING.md` matches at least 1 line
    - `grep -F 'templates/' CONTRIBUTING.md` matches at least 1 line
    - `grep -F 'bin/' CONTRIBUTING.md` matches at least 1 line
    - `grep -F 'bats tests/install.bats' CONTRIBUTING.md` matches at least 1 line
    - `grep -F 'bash scripts/check-parity.sh' CONTRIBUTING.md` matches at least 1 line
    - The Tag protection H3 unchanged: `grep -cE '^### Tag protection' CONTRIBUTING.md` outputs `1`
  </acceptance_criteria>
  <verify>
    <automated>set -e; cd /Users/sylorei/pet-projects/claude-godmode; ! grep -qF 'File Structure (v1.4)' CONTRIBUTING.md &amp;&amp; grep -qF 'File Structure (v2.0)' CONTRIBUTING.md &amp;&amp; grep -qF '.claude-plugin/' CONTRIBUTING.md &amp;&amp; grep -qF '.github/workflows/' CONTRIBUTING.md &amp;&amp; grep -qF 'config/quality-gates.txt' CONTRIBUTING.md &amp;&amp; grep -qF 'templates/' CONTRIBUTING.md &amp;&amp; grep -qF 'bin/' CONTRIBUTING.md &amp;&amp; grep -qF 'bats tests/install.bats' CONTRIBUTING.md &amp;&amp; test "$(grep -cE '^### Tag protection' CONTRIBUTING.md)" = "1"</automated>
  </verify>
  <done>v1.4 block replaced; v2.0 block enumerates real directories; Tag protection H3 preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Replace Model Selection (Four-Tier Strategy) with v2 12-agent enumeration + xhigh pitfall</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - CONTRIBUTING.md (lines 86-114 region — the Model Selection + Quality + decision-tree block)
    - CLAUDE.md § "Default model assignments (v2 — see `.planning/research/STACK.md`)" — verbatim source for the xhigh pitfall and tier policy
    - agents/*.md frontmatter (already extracted in this plan's `<interfaces>` block)
  </read_first>
  <action>
Replace the entire Model Selection section through the decision-tree note (currently lines 86-106 in the file, including the "**Note:** `effort: max` is an Opus-exclusive setting..." paragraph).

old_string (verbatim — full block from `### Model Selection` heading through the `effort: max` Note paragraph):
```
### Model Selection (Four-Tier Strategy)

Agents are assigned to one of four tiers based on task complexity and cost:

- **Opus + high effort** -- High-stakes read-only analysis requiring maximum thoroughness. These agents evaluate architecture and security where missed issues are costly.
  - `@architect`, `@security-auditor`
- **Opus + default effort** -- Code-writing agents that need Opus-level reasoning to produce correct implementations but don't need the thoroughness overhead.
  - `@writer`, `@executor`
- **Sonnet + high effort** -- Structured analysis and generation tasks. Sonnet handles these well when given high effort to be thorough.
  - `@reviewer`, `@test-writer`, `@doc-writer`
- **Sonnet + default effort** -- Background research and information gathering where speed and cost matter more than deep reasoning.
  - `@researcher`

**Decision tree for placing future agents:**

1. Does the agent write or modify code? -> Opus + default effort
2. Does the agent perform high-stakes read-only analysis (security, architecture)? -> Opus + high effort
3. Does the agent produce structured output (reviews, tests, docs)? -> Sonnet + high effort
4. Is the agent primarily research or information gathering? -> Sonnet + default effort

> **Note:** `effort: max` is an Opus-exclusive setting. Do not assign it to Sonnet agents. Most agents should use `high` or omit the field (default). Reserve `max` for edge cases where Opus needs to exhaust all reasoning before responding.
```

new_string:
```
### Model Selection (v2 — three tiers, twelve agents)

Every agent declares `model:` (alias: `opus` / `sonnet` / `haiku`) and `effort:` (`high` or `xhigh`) in its frontmatter. Pinned model IDs are forbidden — aliases keep the upgrade path one config edit instead of twelve.

| Tier | Agents | Use for |
|------|--------|---------|
| `opus` + `effort: xhigh` | `@architect`, `@planner`, `@security-auditor`, `@verifier` | Design, audit, and read-only analysis where missed issues are costly. Read-only or read-mostly. |
| `opus` + `effort: high` | `@executor`, `@writer` | Code-writing. Opus-level reasoning, but `xhigh` skips rules on Opus 4.7 -- do NOT use it on `@executor` / `@writer` / `@test-writer`; use `high` there. |
| `sonnet` + `effort: high` | `@code-reviewer`, `@doc-writer`, `@researcher`, `@reviewer`, `@spec-reviewer`, `@test-writer` | Structured review, generation, and research tasks where Sonnet's strength profile fits. |

> **Pitfall:** `xhigh skips rules on Opus 4.7`. Anthropic's documented behavior makes the highest-effort tier ignore rule files on Opus 4.7 -- it is safe for read-only audit work (`@architect`, `@planner`, `@security-auditor`, `@verifier`) but unsafe for any agent that writes code. Code-writing agents must stay on `effort: high`.

**Decision tree for placing future agents:**

1. Does the agent write or modify code? -> `opus` + `effort: high` (e.g., `@executor`, `@writer`, `@test-writer` -- with `isolation: worktree`)
2. Does the agent perform high-stakes read-only analysis (architecture, security, planning, verification)? -> `opus` + `effort: xhigh`
3. Does the agent produce structured review, generation, or research output? -> `sonnet` + `effort: high`
4. Is the agent a trivially-bounded helper (classifier, format checker)? -> `haiku` (default effort)

For skill frontmatter conventions (the contract `/build`, `/plan`, etc. follow), see `rules/godmode-skills.md`. For agent routing ("which agent does what"), see `rules/godmode-routing.md`. The two rule files are split deliberately -- agent identity vs. skill plumbing.
```
  </action>
  <acceptance_criteria>
    - `grep -F 'effort: max' CONTRIBUTING.md` matches no lines (exit 1)
    - `grep -F 'xhigh skips rules on Opus 4.7' CONTRIBUTING.md` matches at least 1 line
    - `grep -cE 'effort: (high|xhigh)' CONTRIBUTING.md` outputs at least `6`
    - `grep -c '@architect' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@security-auditor' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@planner' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@verifier' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@executor' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@writer' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@test-writer' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@reviewer' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@spec-reviewer' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@code-reviewer' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@researcher' CONTRIBUTING.md` outputs at least `1`
    - `grep -c '@doc-writer' CONTRIBUTING.md` outputs at least `1`
    - `grep -F 'rules/godmode-skills.md' CONTRIBUTING.md` matches at least 1 line (skill-conventions pointer added)
    - `grep -cE '^### Tag protection' CONTRIBUTING.md` outputs `1` (preserved)
    - `grep -cF 'For installation and usage, see [README.md](README.md)' CONTRIBUTING.md` outputs `1` (preserved)
    - `bash scripts/check-vocab.sh; echo $?` outputs `0` (no vocab regression in the repo)
  </acceptance_criteria>
  <verify>
    <automated>set -e; cd /Users/sylorei/pet-projects/claude-godmode; ! grep -qF 'effort: max' CONTRIBUTING.md &amp;&amp; grep -qF 'xhigh skips rules on Opus 4.7' CONTRIBUTING.md &amp;&amp; test "$(grep -cE 'effort: (high|xhigh)' CONTRIBUTING.md)" -ge 6 &amp;&amp; for a in architect security-auditor planner verifier executor writer test-writer reviewer spec-reviewer code-reviewer researcher doc-writer; do grep -q "@${a}" CONTRIBUTING.md || { echo "MISSING @${a}"; exit 1; }; done &amp;&amp; grep -qF 'rules/godmode-skills.md' CONTRIBUTING.md &amp;&amp; bash scripts/check-vocab.sh</automated>
  </verify>
  <done>Model Selection section enumerates all 12 v2 agents with correct (model, effort) tiers; Opus-4.7 xhigh pitfall documented verbatim from CLAUDE.md; `effort: max` references gone; vocab gate still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CONTRIBUTING.md → new contributors | Misdocumented frontmatter or routing produces inconsistent agent files. The vocab gate exempts CONTRIBUTING (D-12), so a code-quality issue lands silently if doc says wrong things. |
| `effort:` field documentation → Opus 4.7 runtime | Telling contributors to use `xhigh` on a code-writing agent silently disables rule files on that agent's runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Information Disclosure / Misdirection | Stale `effort: max` reference | mitigate | Replace with the verbatim `xhigh skips rules on Opus 4.7` pitfall from CLAUDE.md. New contributors see the right tier policy. |
| T-05-07-02 | Tampering | Skill conventions pointed at wrong rule file | mitigate | Pointer corrected to `rules/godmode-skills.md`. |
| T-05-07-03 | Denial of Service | Contributors miss CI gates because File Structure block omits `scripts/` and `tests/` | mitigate | v2 layout block enumerates `scripts/`, `tests/`, `.github/workflows/` and includes the local-run command list. |
| T-05-07-04 | Repudiation | "Light-touch" Plan 05-03 only updated 2 anchors; rest of file misdirects | accept | This plan completes the rewrite that was deferred. |
</threat_model>

<verification>
After all 3 tasks complete, the closure assertion is:

```bash
# CR-04 closure assertions (every check must pass)
! grep -qF 'File Structure (v1.4)' CONTRIBUTING.md && \
  grep -qF 'File Structure (v2.0)' CONTRIBUTING.md && \
  ! grep -qF 'effort: max' CONTRIBUTING.md && \
  grep -qF 'xhigh skips rules on Opus 4.7' CONTRIBUTING.md && \
  grep -qF 'rules/godmode-skills.md' CONTRIBUTING.md && \
  for a in architect security-auditor planner verifier executor writer test-writer reviewer spec-reviewer code-reviewer researcher doc-writer; do
    grep -q "@${a}" CONTRIBUTING.md || { echo "MISSING @${a}"; exit 1; }
  done && \
  test "$(grep -cF 'For installation and usage, see [README.md](README.md)' CONTRIBUTING.md)" = "1" && \
  test "$(grep -cE '^### Tag protection' CONTRIBUTING.md)" = "1" && \
  bash scripts/check-vocab.sh

echo "CR-04 closure: PASS"
```

Expected exit: `0` and the line `CR-04 closure: PASS`.
</verification>

<success_criteria>
- v1.4 file structure heading and tree gone; v2.0 block enumerates real v2 directories
- All 12 v2 agents named at least once with correct (model, effort) tiers
- `effort: max` references removed; `xhigh skips rules on Opus 4.7` pitfall documented
- Skill-conventions pointer corrected to `rules/godmode-skills.md`
- Plan 05-03 anchors preserved (README pointer at top, Tag protection H3)
- Vocab gate stays green (`bash scripts/check-vocab.sh` exits 0)
- Closes CR-04 from `05-VERIFICATION.md`. No QUAL-XX requirement re-affirmed (CR-04 is a phase-contract gap, not a QUAL requirement gap).
</success_criteria>

<output>
After completion, create `.planning/phases/05-quality-ci-tests-docs-parity/05-07-SUMMARY.md` recording: the per-section diff summaries, the closure-assertion command output, and the final CONTRIBUTING.md line count.
</output>
