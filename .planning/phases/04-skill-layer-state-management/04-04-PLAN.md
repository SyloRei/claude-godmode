---
phase: 04-skill-layer-state-management
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - skills/debug/SKILL.md
  - skills/tdd/SKILL.md
  - skills/refactor/SKILL.md
  - skills/explore-repo/SKILL.md
  - skills/prd/SKILL.md
  - skills/plan-stories/SKILL.md
  - skills/execute/SKILL.md
  - rules/godmode-routing.md
autonomous: true
requirements:
  - WORKFLOW-01
  - WORKFLOW-09
  - WORKFLOW-10
  - WORKFLOW-11
user_setup: []

must_haves:
  truths:
    - "The 4 cross-cutting helpers (`/debug`, `/tdd`, `/refactor`, `/explore-repo`) have v2 frontmatter (D-04 keys; no `model:` / `effort:`); have a `## Connects to` section (D-06); have an `## Auto Mode check` block (D-08, D-09); have scoped `allowed-tools` (no wildcards)."
    - "The 4 helpers preserve their v1.x body content semantically (D-53 — major rewrites deferred to v2.1)."
    - "The 3 v1.x deprecation skills (`/prd`, `/plan-stories`, `/execute`) have a deprecation banner block PREPENDED above their preserved v1.x body (D-23) with a one-time marker check at `~/.claude/.claude-godmode-v1-banner-shown` (D-24)."
    - "The migration table in each banner maps `/prd` → `/brief N`, `/plan-stories` → `/plan N`, `/execute` → `/build N`."
    - "The user-facing surface count is exactly 11: `commands/godmode.md` + 10 `skills/{mission,brief,plan,build,verify,ship,debug,tdd,refactor,explore-repo}/SKILL.md`. The 3 deprecated v1.x skills exist on disk but are NOT counted (D-03)."
    - "`rules/godmode-routing.md` is touched to align v2 vocabulary (no leftover `/prd → /plan-stories → /execute → /ship` references in user-facing routing language; the v2 chain is canonical)."
    - "No vocabulary leakage in any of the 4 modernized helpers: `phase`/`story`/`PRD`/`cycle`/`gsd-`/`milestone` absent from user-facing prose. The deprecated 3 skills can use `/prd`/`/plan-stories`/`/execute` ONLY in the banner migration table (legitimate reference to deprecated commands users may type)."
  artifacts:
    - path: "skills/debug/SKILL.md"
      provides: "Debug helper modernized to v2 (Auto Mode + Connects-to + scoped tools)"
      contains: "## Connects to"
    - path: "skills/tdd/SKILL.md"
      provides: "TDD helper modernized to v2"
      contains: "## Connects to"
    - path: "skills/refactor/SKILL.md"
      provides: "Refactor helper modernized to v2"
      contains: "## Connects to"
    - path: "skills/explore-repo/SKILL.md"
      provides: "Repo exploration helper modernized to v2"
      contains: "## Connects to"
    - path: "skills/prd/SKILL.md"
      provides: "v1.x deprecation banner + preserved v1.x body"
      contains: "Deprecated"
    - path: "skills/plan-stories/SKILL.md"
      provides: "v1.x deprecation banner + preserved v1.x body"
      contains: "Deprecated"
    - path: "skills/execute/SKILL.md"
      provides: "v1.x deprecation banner + preserved v1.x body"
      contains: "Deprecated"
    - path: "rules/godmode-routing.md"
      provides: "v2 chain references; no leftover v1.x pipeline language"
      contains: "/brief"
  key_links:
    - from: "skills/{prd,plan-stories,execute}/SKILL.md"
      to: "~/.claude/.claude-godmode-v1-banner-shown"
      via: "test -f marker check; touch on first display"
      pattern: "claude-godmode-v1-banner-shown"
    - from: "skills/{debug,tdd,refactor,explore-repo}/SKILL.md"
      to: "rules/godmode-skills.md"
      via: "Auto Mode block reference"
      pattern: "Auto Mode Active"
---

<objective>
Close out Phase 4 by:
1. Modernizing the 4 cross-cutting helpers (`/debug`, `/tdd`, `/refactor`, `/explore-repo`) to v2 shape — preserve body content semantically, update frontmatter + add Connects-to + Auto Mode block (WORKFLOW-09).
2. Adding v1.x deprecation banners to `/prd`, `/plan-stories`, `/execute` with one-time marker mechanic (WORKFLOW-10).
3. Verifying the 11-skill surface count is exact (WORKFLOW-01).
4. Light vocabulary alignment in `rules/godmode-routing.md` so internal routing docs match the v2 chain.
5. Ensuring every workflow + helper skill includes the canonical Auto Mode block (WORKFLOW-11).

Purpose: After 04-04 ships, the v2 surface is complete and counted. Plans 04-02, 04-03 already shipped the workflow chain; 04-04 ships the helpers and the deprecation story so v1.x users have a graceful migration path.

Output: 4 modernized helper SKILL.md files (semantically preserved v1.x bodies + v2 frontmatter additions), 3 deprecation-banner-prepended v1.x SKILL.md files, light edits to `rules/godmode-routing.md`, and a verification task that asserts the 11-skill surface count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-skill-layer-state-management/04-CONTEXT.md
@.planning/phases/04-skill-layer-state-management/04-RESEARCH.md
@.planning/phases/04-skill-layer-state-management/04-PATTERNS.md
@.planning/phases/04-skill-layer-state-management/04-01-PLAN.md
@skills/debug/SKILL.md
@skills/tdd/SKILL.md
@skills/refactor/SKILL.md
@skills/explore-repo/SKILL.md
@skills/prd/SKILL.md
@skills/plan-stories/SKILL.md
@skills/execute/SKILL.md
@rules/godmode-routing.md
@rules/godmode-skills.md
@CLAUDE.md
@IDEA.md

<interfaces>
<!-- These are the contracts already shipped by Plans 04-01..04-03 that this plan references. -->

From Plan 04-01: rules/godmode-skills.md exists with the canonical Auto Mode block + reserved-slot doctrine + frontmatter convention reference. The 4 helpers + 7 v2 workflow skills all reference it.

The 11-skill surface (counted by WORKFLOW-01 SC #1):
- `commands/godmode.md` (1)
- `skills/mission/SKILL.md` (2) — Plan 04-02
- `skills/brief/SKILL.md` (3) — Plan 04-03
- `skills/plan/SKILL.md` (4) — Plan 04-03
- `skills/build/SKILL.md` (5) — Plan 04-03
- `skills/verify/SKILL.md` (6) — Plan 04-03
- `skills/ship/SKILL.md` (7) — Plan 04-03
- `skills/debug/SKILL.md` (8) — this plan
- `skills/tdd/SKILL.md` (9) — this plan
- `skills/refactor/SKILL.md` (10) — this plan
- `skills/explore-repo/SKILL.md` (11) — this plan

The 3 deprecated v1.x skills (NOT counted toward 11):
- `skills/prd/SKILL.md` — banner-prepended; preserved body
- `skills/plan-stories/SKILL.md` — banner-prepended; preserved body
- `skills/execute/SKILL.md` — banner-prepended; preserved body

Per D-03: deprecated skills are NOT counted because their `description:` field is rewritten to `"[Deprecated v2.0] …"` and (per D-23 banner shape) the user-invocable behavior is REPLACED by a one-line redirect. The frontmatter `user-invocable:` flag may stay `true` (so the slash command still resolves; user types `/prd` and gets the migration banner), but the count of "v2 user-facing surface" is the 11 listed above.

The WORKFLOW-01 SC #1 assertion (from ROADMAP.md): `find commands skills -name '*.md' -type f | grep '^commands/godmode.md\|/SKILL.md$' | wc -l == 11` — but that grep is too lax (it counts deprecated skills too). Refined assertion documented in Task 7 below.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: skills/debug/SKILL.md — modernize to v2 (preserve body)</name>
  <files>skills/debug/SKILL.md</files>
  <read_first>
    - skills/debug/SKILL.md (CURRENT v1.x — entire file) — preserve body content per D-53
    - .planning/phases/04-skill-layer-state-management/04-CONTEXT.md D-52, D-53 (modernization checklist)
    - .planning/phases/04-skill-layer-state-management/04-PATTERNS.md § "skills/{debug,tdd,refactor,explore-repo}/SKILL.md (modernize)" (lines 597-619) — modernization checklist
    - .planning/phases/04-skill-layer-state-management/04-RESEARCH.md § "Recommended `allowed-tools`" (lines 585-606 — `/debug` row: `Read, Grep, Glob, Bash(*)`)
    - rules/godmode-skills.md (Plan 04-01 — Auto Mode block, frontmatter convention, Connects-to format)
  </read_first>
  <action>
    Modernize `skills/debug/SKILL.md`. Operation:
    1. **Preserve the body** (the 4-phase debug protocol or whatever the v1.x body contains).
    2. **Replace frontmatter** with v2 D-04 conformant keys.
    3. **Add `## Connects to` section** right after H1 (per D-06).
    4. **Add `## Auto Mode check` block** after Connects-to (per D-08, D-09).
    5. **Strip pipeline-context block** if present (lines like `<!-- canonical: skills/_shared/pipeline-context.md -->` and the `## Pipeline Context` section). Replace with init-context.sh source if state-aware behavior is needed (debug doesn't need it — leave nothing).
    6. **Vocabulary scrub:** remove any `phase`, `task`, `story`, `PRD`, `gsd-`, `cycle`, `milestone` literal in user-facing prose. The body's debug protocol vocabulary (e.g., "step 1: reproduce") is fine; only the workflow vocabulary leaks.

    EXACT frontmatter shape (per D-04 + RESEARCH § allowed-tools / debug row):
    ```yaml
    ---
    name: debug
    description: "Structured debugging protocol: reproduce → isolate → fix → verify. Free-form Bash for investigation; Read/Grep/Glob for source navigation."
    user-invocable: true
    allowed-tools:
      - Read
      - Grep
      - Glob
      - Bash
    ---
    ```
    NO `model:` / `effort:` (D-05). NO `argument-hint` / `arguments` (helper takes no `N` argument).

    EXACT additions immediately after the H1 (`# Debug` or whatever the v1.x H1 is):

    ```markdown
    ## Connects to

    - **Upstream:** (entry point — cross-cutting helper)
    - **Downstream:** freeform — calls into source files; no agent spawned
    - **Reads from:** working tree, test output, error logs
    - **Writes to:** working tree (when fix is applied)

    ## Auto Mode check

    Scan for "Auto Mode Active" (case-insensitive). When detected: skip
    confirmation prompts on hypothesis testing; pick the most plausible failure
    cause first; surface assumptions inline. See `rules/godmode-skills.md` § Auto Mode Detection.

    ---

    [v1.x debug body preserved verbatim from here]
    ```

    Body preservation rule: read the existing file's content (after frontmatter), preserve it semantically. Only modify the body if it contains:
    - Vocabulary leakage (`phase`, `story`, `PRD`, `cycle`, `gsd-`) — replace with v2 vocabulary or rewrite the offending sentence.
    - Pipeline-context block (`<!-- canonical: skills/_shared/pipeline-context.md -->` and the `## Pipeline Context` section) — REMOVE entirely (no equivalent needed for /debug).
    - References to `.claude-pipeline/stories.json` — REMOVE (debug doesn't read pipeline state).

    The 4-phase / 4-step / N-step debug protocol stays semantically identical (D-53).
  </action>
  <verify>
    <automated>set -e; test -f skills/debug/SKILL.md && grep -q '^name: debug' skills/debug/SKILL.md && grep -q '^user-invocable: true' skills/debug/SKILL.md && grep -q '^## Connects to' skills/debug/SKILL.md && grep -q 'Auto Mode Active' skills/debug/SKILL.md && [ "$(grep -cE '^model:|^effort:' skills/debug/SKILL.md)" = "0" ] && [ "$(grep -ciE '\b(story|PRD|cycle|gsd-|stories\.json|.claude-pipeline)\b' skills/debug/SKILL.md)" = "0" ] && [ "$(grep -E '^[^<#-]*\b(phase|milestone)\b' skills/debug/SKILL.md | wc -l | tr -d ' ')" = "0" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists, frontmatter has D-04 conformant keys (name, description, user-invocable, allowed-tools)
    - `grep -E '^model:|^effort:' skills/debug/SKILL.md | wc -l | tr -d ' '` == 0 (D-05)
    - `grep -E '^argument-hint:|^arguments:' skills/debug/SKILL.md | wc -l | tr -d ' '` == 0 (no N argument)
    - Connects-to has 4 bullets: `grep -cE '^- \*\*(Upstream|Downstream|Reads from|Writes to):\*\*' skills/debug/SKILL.md` == 4
    - Auto Mode block present
    - Pipeline-context block REMOVED: `grep -ciE 'stories\.json|.claude-pipeline|pipeline-context' skills/debug/SKILL.md` == 0
    - `grep -ciE '\b(story|PRD|cycle|gsd-)\b' skills/debug/SKILL.md` == 0
    - `phase` / `milestone` not in user-facing prose (allowing only documentation references): `grep -E '^[^<#-]*\b(phase|milestone)\b' skills/debug/SKILL.md | wc -l | tr -d ' '` == 0
    - Body content (v1.x debug protocol) substantively preserved — measured by line count: the new file's body (after Connects-to and Auto Mode block, ~10 lines added) should be roughly v1.x line count + ~15 lines.
  </acceptance_criteria>
  <done>skills/debug/SKILL.md modernized: v2 frontmatter + Connects-to + Auto Mode block; pipeline-context stripped; vocabulary clean; body semantically preserved.</done>
</task>

<task type="auto">
  <name>Task 2: skills/tdd/SKILL.md — modernize to v2 (preserve body)</name>
  <files>skills/tdd/SKILL.md</files>
  <read_first>
    - skills/tdd/SKILL.md (CURRENT v1.x)
    - .planning/phases/04-skill-layer-state-management/04-CONTEXT.md D-52, D-53
    - .planning/phases/04-skill-layer-state-management/04-PATTERNS.md § modernize block (lines 597-619)
    - .planning/phases/04-skill-layer-state-management/04-RESEARCH.md § allowed-tools `/tdd` row (Read, Write, Edit, Bash, Task(test-writer))
    - rules/godmode-skills.md (Plan 04-01)
    - agents/test-writer.md (Phase 2 — `/tdd` may spawn this for test scaffolding)
  </read_first>
  <action>
    Same operation as Task 1, applied to `skills/tdd/SKILL.md`.

    Frontmatter (D-04 + RESEARCH § allowed-tools / tdd row):
    ```yaml
    ---
    name: tdd
    description: "Test-first development: red → green → refactor. May spawn @test-writer for test scaffolding."
    user-invocable: true
    allowed-tools:
      - Read
      - Write
      - Edit
      - Bash
      - Task
    ---
    ```
    NOTE: `Task(test-writer)` per RESEARCH; if Task tool requires the simple form `Task` in `allowed-tools` (not `Task(test-writer)`), use `Task` — Claude Code accepts both forms (verify against latest docs; default to `Task` simple form for portability).

    Connects-to (per D-52):
    ```markdown
    ## Connects to

    - **Upstream:** (entry point — cross-cutting helper)
    - **Downstream:** @test-writer (optional — scaffolds tests)
    - **Reads from:** working tree (source under test)
    - **Writes to:** test files, source files (red→green cycle)
    ```

    Auto Mode block (verbatim from rules/godmode-skills.md canonical D-08 block).

    Body: preserve v1.x red/green/refactor protocol semantically. Strip any pipeline-context references.
  </action>
  <verify>
    <automated>set -e; test -f skills/tdd/SKILL.md && grep -q '^name: tdd' skills/tdd/SKILL.md && grep -q '^## Connects to' skills/tdd/SKILL.md && grep -q 'Auto Mode Active' skills/tdd/SKILL.md && [ "$(grep -cE '^model:|^effort:' skills/tdd/SKILL.md)" = "0" ] && [ "$(grep -ciE '\b(story|PRD|cycle|gsd-|stories\.json|.claude-pipeline)\b' skills/tdd/SKILL.md)" = "0" ] && [ "$(grep -cE '^- \*\*(Upstream|Downstream|Reads from|Writes to):\*\*' skills/tdd/SKILL.md)" = "4" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Same checks as Task 1, applied to `skills/tdd/SKILL.md`
    - Pipeline-context REMOVED
    - 4 Connects-to bullets present
    - Auto Mode block present
    - No vocabulary leakage
    - V1.x TDD body content semantically preserved (red/green/refactor steps survive)
  </acceptance_criteria>
  <done>skills/tdd/SKILL.md modernized.</done>
</task>

<task type="auto">
  <name>Task 3: skills/refactor/SKILL.md — modernize to v2 (preserve body)</name>
  <files>skills/refactor/SKILL.md</files>
  <read_first>
    - skills/refactor/SKILL.md (CURRENT v1.x)
    - Same context as Task 1 (CONTEXT.md D-52/D-53; PATTERNS modernize block; RESEARCH allowed-tools / refactor row: `Read, Write, Edit, Bash, Task(architect)`)
    - rules/godmode-skills.md
    - agents/architect.md (Phase 2 modernized — `/refactor` may spawn for design review)
  </read_first>
  <action>
    Same modernization as Tasks 1-2, applied to `skills/refactor/SKILL.md`.

    Frontmatter:
    ```yaml
    ---
    name: refactor
    description: "Safe refactoring with test verification: identify smell → propose change → verify behavior. May spawn @architect for design review."
    user-invocable: true
    allowed-tools:
      - Read
      - Write
      - Edit
      - Bash
      - Task
    ---
    ```

    Connects-to:
    ```markdown
    ## Connects to

    - **Upstream:** (entry point — cross-cutting helper)
    - **Downstream:** @architect (optional — design review)
    - **Reads from:** working tree, test suite
    - **Writes to:** working tree (refactored source); tests preserve behavior
    ```

    Auto Mode block (canonical).

    Body: preserve v1.x refactor protocol. Strip pipeline-context. Per PATTERNS line 615: existing `skills/refactor/SKILL.md:122-156` (if present) — REMOVE; nothing replaces.
  </action>
  <verify>
    <automated>set -e; test -f skills/refactor/SKILL.md && grep -q '^name: refactor' skills/refactor/SKILL.md && grep -q '^## Connects to' skills/refactor/SKILL.md && grep -q 'Auto Mode Active' skills/refactor/SKILL.md && [ "$(grep -cE '^model:|^effort:' skills/refactor/SKILL.md)" = "0" ] && [ "$(grep -ciE '\b(story|PRD|cycle|gsd-|stories\.json|.claude-pipeline)\b' skills/refactor/SKILL.md)" = "0" ] && [ "$(grep -cE '^- \*\*(Upstream|Downstream|Reads from|Writes to):\*\*' skills/refactor/SKILL.md)" = "4" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Same as Tasks 1-2, applied to `skills/refactor/SKILL.md`
  </acceptance_criteria>
  <done>skills/refactor/SKILL.md modernized.</done>
</task>

<task type="auto">
  <name>Task 4: skills/explore-repo/SKILL.md — modernize to v2 (preserve body)</name>
  <files>skills/explore-repo/SKILL.md</files>
  <read_first>
    - skills/explore-repo/SKILL.md (CURRENT v1.x)
    - .planning/phases/04-skill-layer-state-management/04-CONTEXT.md D-52, D-53
    - .planning/phases/04-skill-layer-state-management/04-RESEARCH.md § allowed-tools `/explore-repo` row (Read, Grep, Glob, Bash(find *), Bash(git *)) AND § STACK.md "Skills" — `context: fork` + `agent: Plan` is the recommended forking pattern for read-only research
    - rules/godmode-skills.md
  </read_first>
  <action>
    Same modernization as Tasks 1-3, applied to `skills/explore-repo/SKILL.md`.

    Frontmatter (per RESEARCH § allowed-tools and STACK.md skill conventions for read-only forking helpers):
    ```yaml
    ---
    name: explore-repo
    description: "Deep read-only repo exploration: structure, conventions, integration points. Forked context for isolated research."
    user-invocable: true
    allowed-tools:
      - Read
      - Grep
      - Glob
      - Bash
    ---
    ```
    NOTE: `context: fork` is OPTIONAL per RESEARCH — adopt only if v1.x already used it; otherwise, leave standard. Document inline if used.

    Connects-to:
    ```markdown
    ## Connects to

    - **Upstream:** (entry point — cross-cutting helper)
    - **Downstream:** freeform — produces a structured report
    - **Reads from:** working tree, git log, file structure
    - **Writes to:** none (read-only exploration; report returned to user)
    ```

    Auto Mode block (canonical).

    Body: preserve v1.x exploration protocol. Strip pipeline-context.
  </action>
  <verify>
    <automated>set -e; test -f skills/explore-repo/SKILL.md && grep -q '^name: explore-repo' skills/explore-repo/SKILL.md && grep -q '^## Connects to' skills/explore-repo/SKILL.md && grep -q 'Auto Mode Active' skills/explore-repo/SKILL.md && [ "$(grep -cE '^model:|^effort:' skills/explore-repo/SKILL.md)" = "0" ] && [ "$(grep -ciE '\b(story|PRD|cycle|gsd-|stories\.json|.claude-pipeline)\b' skills/explore-repo/SKILL.md)" = "0" ] && [ "$(grep -cE '^- \*\*(Upstream|Downstream|Reads from|Writes to):\*\*' skills/explore-repo/SKILL.md)" = "4" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Same as Tasks 1-3
  </acceptance_criteria>
  <done>skills/explore-repo/SKILL.md modernized.</done>
</task>

<task type="auto">
  <name>Task 5: skills/{prd,plan-stories,execute}/SKILL.md — prepend v1.x deprecation banner</name>
  <files>skills/prd/SKILL.md, skills/plan-stories/SKILL.md, skills/execute/SKILL.md</files>
  <read_first>
    - skills/prd/SKILL.md, skills/plan-stories/SKILL.md, skills/execute/SKILL.md (current v1.x — preserve bodies)
    - .planning/phases/04-skill-layer-state-management/04-CONTEXT.md D-23, D-24, D-25
    - .planning/phases/04-skill-layer-state-management/04-RESEARCH.md § "v1.x deprecation banner one-time mechanic (D-23, D-24)" (lines 705-746) — full banner template + marker file location rationale
    - .planning/phases/04-skill-layer-state-management/04-PATTERNS.md § "skills/{prd,plan-stories,execute}/SKILL.md (deprecation banner)" (lines 623-666)
    - skills/_shared/gitignore-management.md (idempotent-check idiom)
  </read_first>
  <action>
    For each of the 3 files (`skills/prd/SKILL.md`, `skills/plan-stories/SKILL.md`, `skills/execute/SKILL.md`), perform this transformation:

    1. **Read** the existing v1.x file.
    2. **Update frontmatter** to mark deprecated:
       ```yaml
       ---
       name: <prd|plan-stories|execute>
       description: "[Deprecated v2.0] Renamed to <new>. See migration note below. Old behavior preserved for v1.x users mid-migration."
       user-invocable: true
       ---
       ```
       The description for each is exactly:
       - `prd`: `"[Deprecated v2.0] Renamed to /brief N. See migration note below. Old behavior preserved for v1.x users mid-migration."`
       - `plan-stories`: `"[Deprecated v2.0] Renamed to /plan N. See migration note below. Old behavior preserved for v1.x users mid-migration."`
       - `execute`: `"[Deprecated v2.0] Renamed to /build N. See migration note below. Old behavior preserved for v1.x users mid-migration."`

       Note: keep `user-invocable: true` so the slash command still resolves; the banner is the user's first encounter.

       Strip any v1.x `model:` / `effort:` / `allowed-tools:` keys to keep the deprecated frontmatter minimal — the banner exits early in most cases. Tools needed by the v1.x body (if user proceeds past banner) are inherited from session defaults.

    3. **Prepend the banner block** between the closing `---` of the frontmatter and the H1 of the v1.x body. Banner shape per D-23 (verbatim):

       ```markdown
       <!-- v2.0 DEPRECATION BANNER — display once per install. Marker file gates display. -->

       ## v2.0 Migration Note

       Check the marker file:
       ```bash
       MARKER="$HOME/.claude/.claude-godmode-v1-banner-shown"
       if [ ! -f "$MARKER" ]; then
         # Display banner block below
         touch "$MARKER" 2>/dev/null || true
         # Then continue to v1.x body
       fi
       # If marker exists, skip the banner and proceed straight to v1.x body
       ```

       # ⚠ Deprecated — use `/brief N` instead

       This command was renamed in v2.0:

       | v1.x | v2.0 |
       |---|---|
       | `/prd` | `/brief N` |
       | `/plan-stories` | `/plan N` |
       | `/execute` | `/build N` |

       The old body still works for projects on the v1.x layout (`.claude-pipeline/`).
       Run `/mission` to migrate to the v2 layout (`.planning/`).

       Banner shown once per install — re-display by running:

       ```bash
       rm ~/.claude/.claude-godmode-v1-banner-shown
       ```

       --- v1.x body below ---
       ```

       Per-skill: change the H1 in the banner ("⚠ Deprecated — use `/brief N` instead") to match the migration target:
       - `prd` → "⚠ Deprecated — use `/brief N` instead"
       - `plan-stories` → "⚠ Deprecated — use `/plan N` instead"
       - `execute` → "⚠ Deprecated — use `/build N` instead"

       The migration TABLE inside the banner is identical across all 3 (lists all 3 mappings — so users who type any of them learn about the others).

    4. **Preserve the v1.x body verbatim** below the `--- v1.x body below ---` separator. Including any v1.x H1, sections, code blocks. The banner is OPAQUE to the v1.x body.

    5. **Marker file rationale (D-24):** the marker lives at `~/.claude/.claude-godmode-v1-banner-shown` (NOT under `${CLAUDE_PLUGIN_DATA}`). Per RESEARCH § Banner one-time mechanic: the marker is user-scoped (must persist across plugin reinstalls but is not plugin-config). Documented inline in the banner block.

    Per D-25, banners are removed in v2.x — that's a CHANGELOG entry for v2.x; Phase 4 just ships them.
  </action>
  <verify>
    <automated>set -e; for s in prd plan-stories execute; do test -f "skills/$s/SKILL.md" || { echo "MISSING: $s"; exit 1; }; grep -q 'Deprecated v2.0' "skills/$s/SKILL.md" || { echo "NO BANNER DESCRIPTION: $s"; exit 1; }; grep -q 'claude-godmode-v1-banner-shown' "skills/$s/SKILL.md" || { echo "NO MARKER: $s"; exit 1; }; grep -q '/brief N\|/plan N\|/build N' "skills/$s/SKILL.md" || { echo "NO MIGRATION TABLE: $s"; exit 1; }; grep -q 'v1.x body below' "skills/$s/SKILL.md" || { echo "NO BODY SEPARATOR: $s"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist
    - Each frontmatter description starts with `[Deprecated v2.0]`
    - Each contains the marker file path: `claude-godmode-v1-banner-shown`
    - Each contains the migration TABLE with all 3 mappings: `grep -c '/prd' skills/prd/SKILL.md` ≥ 1; `grep -c '/plan-stories' skills/prd/SKILL.md` ≥ 1; `grep -c '/execute' skills/prd/SKILL.md` ≥ 1; same for the other two files
    - Each contains the body separator `--- v1.x body below ---`
    - The v1.x body content is preserved (line count of new file > old file by at least the banner length, ~30 lines added)
    - Marker location is `$HOME/.claude/.claude-godmode-v1-banner-shown` NOT `${CLAUDE_PLUGIN_DATA}`: `grep -c 'CLAUDE_PLUGIN_DATA.*banner-shown' skills/prd/SKILL.md` == 0; `grep -c 'HOME/.claude/.claude-godmode-v1-banner-shown\|~/.claude/.claude-godmode-v1-banner-shown' skills/prd/SKILL.md` ≥ 1
    - Each banner H1 names the v2 replacement: prd → `/brief N`, plan-stories → `/plan N`, execute → `/build N`
  </acceptance_criteria>
  <done>3 v1.x skills have banner-prepended bodies; marker mechanic in place; v1.x body content preserved verbatim below the separator.</done>
</task>

<task type="auto">
  <name>Task 6: rules/godmode-routing.md — light v2 vocabulary alignment</name>
  <files>rules/godmode-routing.md</files>
  <read_first>
    - rules/godmode-routing.md (CURRENT v1.x — Phase 2 modernized)
    - .planning/phases/02-agent-layer-modernization/02-CONTEXT.md OUT-01 (Phase 2 deferred full vocab realignment to Phase 4)
    - .planning/phases/04-skill-layer-state-management/04-CONTEXT.md (this phase's vocabulary discipline)
    - rules/godmode-skills.md (Plan 04-01 — companion file)
  </read_first>
  <action>
    Read the current `rules/godmode-routing.md`. Goal: ensure the file references the v2 chain (`/godmode → /mission → /brief N → /plan N → /build N → /verify N → /ship`) and not the v1.x chain (`/prd → /plan-stories → /execute → /ship`).

    Operations:
    1. **Search-and-replace (where contextually correct)** any literal `/prd → /plan-stories → /execute → /ship` chain references with the v2 chain.
    2. **Strip references** to `.claude-pipeline/stories.json` from user-facing routing language (if any).
    3. **Add a "Skills companion"** sentence near the top: `See rules/godmode-skills.md for skill frontmatter convention, Auto Mode detection, and Connects-to chain rendering.`
    4. **Preserve agent routing tables** — Phase 2 already modernized these; do not re-modify.
    5. **Vocabulary scrub** in user-facing prose only (not in agent definitions or technical specs):
       - `phase` (workflow context) → `wave` or omit
       - `story` → `brief` or `task` (depending on context — story in v1.x meant a unit of work; in v2, `task` is the atomic unit inside PLAN.md)
       - `PRD` → `brief` (if used to mean the document) or `BRIEF.md` (if used to mean the file)
       - `cycle` → omit
       - `gsd-` → omit (these are GSD-internal references; this is the plugin product, not GSD)
       - `milestone` — KEEP if used for project-level milestone vocabulary (legitimate v2 term per IDEA.md 5-concept hierarchy: Project → Mission → Brief → Plan → Commit; milestone is the parent of brief)

    The change is light-touch — Phase 2 already did the major work. Phase 4 just ensures the v2 chain is canonical here.

    DO NOT rewrite agent frontmatter conventions in this file (Phase 2 owns that).
    DO NOT add new sections; this is alignment-only.

    If the file already references the v2 chain (Phase 2 may have done it preemptively), this task may be a no-op verification. Read the file; if no v1.x leakage exists, the task succeeds with no edits.
  </action>
  <verify>
    <automated>set -e; test -f rules/godmode-routing.md && [ "$(grep -ciE 'stories\.json|/prd[^a-z]|/plan-stories|/execute[^a-z]' rules/godmode-routing.md)" = "0" ] && grep -qE '/brief|/plan|/build|/verify|/ship' rules/godmode-routing.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File exists, was readable
    - No literal v1.x command chain references remain in user-facing prose: `grep -ciE '/prd[^a-z]|/plan-stories|/execute[^a-z]|stories\.json' rules/godmode-routing.md` == 0
    - References v2 chain: at least one of `/brief`, `/plan`, `/build`, `/verify`, `/ship` mentioned: `grep -cE '/brief|/plan|/build|/verify|/ship' rules/godmode-routing.md` ≥ 1
    - Phase 2 agent-routing tables preserved (line count change ≤30 lines from baseline)
  </acceptance_criteria>
  <done>rules/godmode-routing.md aligned to v2 vocabulary; agent routing content preserved.</done>
</task>

<task type="auto">
  <name>Task 7: WORKFLOW-01 surface count assertion (verification + audit document)</name>
  <files>.planning/phases/04-skill-layer-state-management/04-04-SURFACE-AUDIT.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 4 Success Criteria #1 (the find expression)
    - .planning/phases/04-skill-layer-state-management/04-CONTEXT.md D-01, D-03 (which 11; deprecated 3 NOT counted)
    - IDEA.md (the locked 11-command surface)
    - All 11 skill files from Plans 04-02, 04-03, this plan's Tasks 1-4
  </read_first>
  <action>
    This task is a VERIFICATION + AUDIT step that ensures WORKFLOW-01 SC #1 is satisfied. It produces a single document `.planning/phases/04-skill-layer-state-management/04-04-SURFACE-AUDIT.md` that records:

    1. **The 11-skill surface (canonical):**
       1. `commands/godmode.md`
       2. `skills/mission/SKILL.md`
       3. `skills/brief/SKILL.md`
       4. `skills/plan/SKILL.md`
       5. `skills/build/SKILL.md`
       6. `skills/verify/SKILL.md`
       7. `skills/ship/SKILL.md`
       8. `skills/debug/SKILL.md`
       9. `skills/tdd/SKILL.md`
       10. `skills/refactor/SKILL.md`
       11. `skills/explore-repo/SKILL.md`

    2. **The 3 deprecated v1.x skills (NOT in the count):**
       - `skills/prd/SKILL.md`
       - `skills/plan-stories/SKILL.md`
       - `skills/execute/SKILL.md`

    3. **The shared (non-invocable) skills (NOT in the count):**
       - `skills/_shared/*` (directory excluded by `find` filter `-not -name '_*'`)

    4. **Counting recipe** (refined from ROADMAP SC #1):
       The naive `find commands skills -name '*.md' -type f | wc -l` would count 14 (11 + 3 deprecated; _shared dir contains .md helpers too). Refined recipe:
       ```bash
       count=$( {
         # Include commands/godmode.md if present
         find commands -maxdepth 1 -name 'godmode.md' -type f 2>/dev/null
         # Include skills/<name>/SKILL.md where the dir is not _shared and not in the deprecated list
         for d in skills/*/; do
           name=$(basename "$d")
           case "$name" in _*|prd|plan-stories|execute) continue ;; esac
           [ -f "$d/SKILL.md" ] && printf '%s\n' "$d/SKILL.md"
         done
       } | wc -l | tr -d ' ')
       echo "v2 user-facing surface count: $count"
       ```
       Or equivalently using a single find with pruning:
       ```bash
       find commands skills -mindepth 1 \( -name '_shared' -o -name 'prd' -o -name 'plan-stories' -o -name 'execute' \) -prune -o -type f \( -name 'godmode.md' -o -name 'SKILL.md' \) -print | wc -l
       ```

    5. **Document the `task` vocabulary exception** for `/build`, `/verify`, `/ship` (per Plan 04-03 Task 3 note): Phase 5 vocab gate (QUAL-04) needs to whitelist `task` in those 3 files OR remove `task` from the forbidden list (since `task` IS part of v2 vocabulary inside PLAN.md per D-35 template — "Task NN.M" headings).

    6. **Phase 5 hand-off note:** the skill frontmatter linter extension (Phase 5) MUST count exactly 11 user-invocable skills using the refined recipe; the simple `find | grep '/SKILL.md$' | wc -l` would over-count by 3.

    The audit document is written under `.planning/phases/04-skill-layer-state-management/`. It's NOT shipped with the plugin (this is a planning artifact, not a runtime artifact). It serves as the WORKFLOW-01 SC #1 verification trail and as a Phase 5 hand-off contract.
  </action>
  <verify>
    <automated>set -e; AUDIT=.planning/phases/04-skill-layer-state-management/04-04-SURFACE-AUDIT.md; test -f "$AUDIT" && grep -q 'commands/godmode.md' "$AUDIT" && grep -q 'skills/mission/SKILL.md' "$AUDIT" && grep -q 'skills/explore-repo/SKILL.md' "$AUDIT" && grep -q 'skills/prd/SKILL.md' "$AUDIT" && grep -q 'task.*exception\|task.*whitelist\|task.*allowed' "$AUDIT" && COUNT=$(find commands skills -mindepth 1 \( -name '_shared' -o -name 'prd' -o -name 'plan-stories' -o -name 'execute' \) -prune -o -type f \( -name 'godmode.md' -o -name 'SKILL.md' \) -print | wc -l | tr -d ' ') && echo "Surface count: $COUNT" && [ "$COUNT" = "11" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-skill-layer-state-management/04-04-SURFACE-AUDIT.md` exists
    - Document lists all 11 v2 surface skills explicitly
    - Document lists the 3 deprecated v1.x skills (excluded from count)
    - Document includes the refined `find` recipe (the simple ROADMAP SC #1 expression is documented as insufficient)
    - Document includes the `task` exception note for /build, /verify, /ship
    - Live count assertion passes: `find commands skills -mindepth 1 \( -name '_shared' -o -name 'prd' -o -name 'plan-stories' -o -name 'execute' \) -prune -o -type f \( -name 'godmode.md' -o -name 'SKILL.md' \) -print | wc -l | tr -d ' '` equals `11`
    - Document includes a Phase 5 hand-off note describing the linter expectation
  </acceptance_criteria>
  <done>WORKFLOW-01 SC #1 verified: surface count = 11; audit document captures the canonical 11 + the 3 deprecations + Phase 5 hand-off contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user reading deprecation banner → marker file write | banner skill body issues `touch ~/.claude/.claude-godmode-v1-banner-shown` |
| existing v1.x body content → preserved through banner prepend | v1.x body could contain anything; we preserve verbatim |
| `find` based surface count → drift if a new skill dir lands without filter update | the prune list (`prd`, `plan-stories`, `execute`, `_shared`) is fragile to renames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-41 | Spoofing | Marker file `~/.claude/.claude-godmode-v1-banner-shown` deleted by user; banner re-shows | accept | Banner is informational — re-display is harmless. D-24 explicitly documents the re-display recipe (`rm ~/.claude/.claude-godmode-v1-banner-shown`). |
| T-04-42 | Tampering | v1.x body content includes a malicious instruction (e.g., "delete the banner marker") | accept | The v1.x body was authored by the same plugin maintainers. Trust boundary is the plugin install (FOUND-01 enforces). |
| T-04-43 | Information Disclosure | Marker file path leaks user's `$HOME` to terminal | accept | Path is `~/.claude/...` — same locality as all other plugin state. Standard convention; no secret material. |
| T-04-44 | DoS | New skill dir added under `skills/` without updating prune list, surface count silently grows | mitigate | Phase 5 frontmatter linter (QUAL-01) adds a count gate: refuses commit if surface count != 11. Plan 04-04's audit document is the hand-off contract for that gate. |
| T-04-45 | Tampering | Vocabulary leakage in v1.x preserved bodies (which Phase 5 vocab gate exempts via path allow-list per QUAL-04) | accept | Phase 5's vocab gate (QUAL-04) explicitly exempts the 3 deprecated skill paths. v1.x bodies use `phase`/`task`/`story` legitimately and are not user-facing v2 surface. The banner header (above `--- v1.x body below ---`) IS user-facing and must be vocabulary-clean (verified in Task 5 acceptance). |
| T-04-46 | Information Disclosure | Auto Mode block in helper exposes "Auto Mode Active" string to terminal output | accept | This is the canonical detection phrase; intentionally documented and visible. Not a secret. |
| T-04-47 | Tampering | `rules/godmode-routing.md` edit accidentally removes Phase 2's agent routing tables | mitigate | Task 6 acceptance criteria: line-count delta ≤30 lines from baseline (light-touch alignment only). |
</threat_model>

<verification>
After all 7 tasks complete:

1. **All 4 helpers modernized:** `for s in debug tdd refactor explore-repo; do grep -q 'Auto Mode Active' "skills/$s/SKILL.md" && grep -q '## Connects to' "skills/$s/SKILL.md" || echo "FAIL: $s"; done` outputs nothing.

2. **All 3 deprecation banners in place:** `for s in prd plan-stories execute; do grep -q 'Deprecated v2.0' "skills/$s/SKILL.md" && grep -q 'banner-shown' "skills/$s/SKILL.md" || echo "FAIL: $s"; done` outputs nothing.

3. **Surface count exactly 11:** `find commands skills -mindepth 1 \( -name '_shared' -o -name 'prd' -o -name 'plan-stories' -o -name 'execute' \) -prune -o -type f \( -name 'godmode.md' -o -name 'SKILL.md' \) -print | wc -l | tr -d ' '` == `11`.

4. **No vocabulary leakage in helpers OR commands/godmode.md OR workflow skills:** `grep -ciE '\b(story|PRD|cycle|gsd-)\b' commands/godmode.md skills/{mission,brief,plan,build,verify,ship,debug,tdd,refactor,explore-repo}/SKILL.md | awk -F: '$2>0{print}'` outputs nothing.

5. **Connects-to chain renders cleanly:** `grep -lE '^## Connects to' commands/godmode.md skills/*/SKILL.md | wc -l | tr -d ' '` ≥ 11 (every v2 skill has the section; deprecated skills don't need it; _shared/* are not skills).

6. **Auto Mode block in all 7 workflow skills + 4 helpers:** `for s in commands/godmode.md skills/{mission,brief,plan,build,verify,ship,debug,tdd,refactor,explore-repo}/SKILL.md; do grep -q 'Auto Mode Active' "$s" || echo "NO_AUTO: $s"; done` outputs nothing (per WORKFLOW-11).

7. **Surface audit document exists with refined find recipe:** Task 7's verify command passes; document includes Phase 5 hand-off note.
</verification>

<success_criteria>
- All 7 tasks complete; all acceptance_criteria pass
- 4 modernized helpers (`/debug`, `/tdd`, `/refactor`, `/explore-repo`) with v2 frontmatter + Connects-to + Auto Mode block; v1.x body content semantically preserved
- 3 deprecated v1.x skills (`/prd`, `/plan-stories`, `/execute`) with banner prepended + marker mechanic; v1.x body content verbatim below separator
- `rules/godmode-routing.md` aligned to v2 chain (no v1.x leakage in user-facing prose)
- WORKFLOW-01 surface count = 11 (asserted by find recipe in `04-04-SURFACE-AUDIT.md`)
- WORKFLOW-11 satisfied: every workflow + helper skill has Auto Mode block
- All commits use `[brief 04.4]` token
</success_criteria>

<output>
After completion, create `.planning/phases/04-skill-layer-state-management/04-04-SUMMARY.md` documenting:
- The 4 modernized helpers (file paths + a one-line "what changed" per file)
- The 3 deprecated skills with banner-prepend status
- `rules/godmode-routing.md` delta (lines added/removed/changed)
- The 11-skill surface count (CONFIRMED) with the canonical `find` recipe
- Phase 5 hand-off contract: vocabulary gate must whitelist `task` for `skills/{build,verify,ship}/SKILL.md`; surface-count linter must use the refined `find` recipe
- Any deviations (should be none)
</output>

## Tasks — Decision Coverage Map

This plan addresses the following CONTEXT.md decisions: D-01 (skill file location: commands/godmode.md + 10 skills/<name>/SKILL.md split), D-02 (reserved-slot doctrine), D-03 (v1.x bodies preserved during v2.0; counted-out of 11-cap), D-04 (skill frontmatter convention), D-05 (no model/effort at skill level), D-06 (Connects-to body section), D-07 (Connects-to chain), D-08 (Auto Mode canonical detection block), D-09 (per-skill Auto Mode detection), D-10 (Auto Mode recommended-default policy), D-23 (v1.x deprecation banner block prepended to v1.x bodies), D-24 (one-time marker at ~/.claude/.claude-godmode-v1-banner-shown), D-25 (banners removed in v2.x — CHANGELOG note), D-52 (4 cross-cutting helpers v2 lift — Auto Mode + Connects-to + scoped allowed-tools), D-53 (helpers preserve v1.x body semantically; major rewrites deferred).
