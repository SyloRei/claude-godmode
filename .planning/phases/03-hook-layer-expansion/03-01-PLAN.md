---
phase: 03-hook-layer-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/pre-tool-use.sh
  - hooks/hooks.json
  - config/settings.template.json
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-06
user_setup: []

must_haves:
  truths:
    - "hooks/pre-tool-use.sh exists, is executable, shellcheck-clean."
    - "Hook returns permissionDecision: deny for `git commit --no-verify`, `git commit -n`, `--no-gpg-sign`, `git -c commit.gpgsign=false commit`, `git push --force` to main/master."
    - "Hook returns permissionDecision: deny for input containing AWS keys (AKIA...), GitHub PATs (ghp_..., github_pat_...), JWT shapes, or generic api_key= / secret= / password= / token= patterns."
    - "Hook fast-paths to {} for non-Bash tools and for non-matching commands."
    - "Both hooks.json and settings.template.json declare PreToolUse binding with matcher: Bash, timeout: 5."
  artifacts:
    - "hooks/pre-tool-use.sh (new)."
    - "hooks/hooks.json contains PreToolUse block with matcher: Bash."
    - "config/settings.template.json contains PreToolUse block with matcher: Bash."
  key_links:
    - "Refusal output uses jq -n --arg (no heredoc) — closes CR-02 regression risk."
    - "Plugin-mode and manual-mode bindings byte-for-byte agree (Phase 5 QUAL-03 will assert)."
---

<objective>
Ship the mechanical quality-gate enforcement layer. After this plan, `git commit --no-verify` returns `permissionDecision: deny` to Claude Code; hardcoded secrets in tool input are caught and refused. Wires the hook into both plugin-mode (`hooks.json`) and manual-mode (`settings.template.json`) configs with byte-for-byte equivalent shape.

Purpose: HOOK-01 (--no-verify block), HOOK-02 (secret pattern scan), HOOK-06 (parity wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-hook-layer-expansion/03-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

@hooks/session-start.sh
@hooks/post-compact.sh
@hooks/hooks.json
@config/settings.template.json
@config/quality-gates.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Create hooks/pre-tool-use.sh</name>
  <files>hooks/pre-tool-use.sh</files>
  <read_first>hooks/post-compact.sh, .planning/phases/03-hook-layer-expansion/03-CONTEXT.md, .planning/research/STACK.md</read_first>
  <action>Create `hooks/pre-tool-use.sh` per CONTEXT D-01..D-08. Pure bash + jq. Bash 3.2 portable.

```bash
#!/usr/bin/env bash
# hooks/pre-tool-use.sh
# Mechanical quality-gate enforcement: blocks --no-verify, --no-gpg-sign, force-push to main/master,
# and hardcoded secret patterns in tool input. v2.0 has NO bypass — refusal is final.
# (claude-godmode-force-bypass magic phrase reserved for v2.1.)
# Bash 3.2 portable. Closes CR-02 risk by using jq -n --arg for output.
set -euo pipefail

INPUT=$(cat || true)

# Fast-path: only fire on Bash tool dispatch
TOOL_NAME=$(printf '%s' "$INPUT" | jq -r '.tool_name // empty' 2>/dev/null || true)
[ "$TOOL_NAME" = "Bash" ] || { echo '{}'; exit 0; }

CMD=$(printf '%s' "$INPUT" | jq -r '.tool_input.command // empty' 2>/dev/null || true)
[ -n "$CMD" ] || { echo '{}'; exit 0; }

# --- Quality-gate-bypass blocker (HOOK-01) ---
deny() {
  local reason="$1" suggestion="$2"
  jq -n --arg ctx "[godmode-pre-tool-use] BLOCKED: $reason. See rules/godmode-quality.md and config/quality-gates.txt. Use the gates-respecting path: $suggestion." \
    '{hookSpecificOutput: {hookEventName: "PreToolUse", permissionDecision: "deny", additionalContext: $ctx}}'
  exit 0
}

case "$CMD" in
  *"git commit"*"--no-verify"*)
    deny "git commit --no-verify bypasses pre-commit hooks" "fix the failing hook, then commit normally"
    ;;
  *"git commit"*"--no-gpg-sign"*|*"git -c commit.gpgsign=false"*"commit"*)
    deny "GPG signing bypass" "if signing is configured, sign your commit; otherwise unset commit.gpgsign in your local config"
    ;;
esac

# `git commit -n` short form — match the bare flag, not `-n` as part of other flags
case "$CMD" in
  *"git commit "*" -n"|*"git commit "*" -n "*|*"git commit -n"|*"git commit -n "*)
    deny "git commit -n (short form of --no-verify) bypasses pre-commit hooks" "fix the failing hook, then commit normally"
    ;;
esac

# Force push to main/master
case "$CMD" in
  *"git push"*"--force"*"main"*|*"git push"*"--force"*"master"*|*"git push"*" -f "*"main"*|*"git push"*" -f "*"master"*|*"git push"*"-f main"*|*"git push"*"-f master"*)
    deny "git push --force to main/master rewrites shared history" "open a PR; if you must rewrite, use a feature branch"
    ;;
esac

# --- Secret pattern scan (HOOK-02) ---
# Scan the full tool_input as a JSON blob (catches secrets in command, env, etc.)
INPUT_BLOB=$(printf '%s' "$INPUT" | jq -r '.tool_input // {} | tostring' 2>/dev/null || true)

scan_secret() {
  local pattern="$1" label="$2"
  if printf '%s' "$INPUT_BLOB" | grep -qE "$pattern"; then
    deny "$label detected in tool input" "use an env var (export ${label}=...) or read from .env; never hardcode in source"
  fi
}

# AWS access keys
scan_secret 'AKIA[0-9A-Z]{16}' "AWS access key (AKIA...)"
# AWS secret access keys (40-char base64-ish after assignment)
scan_secret 'aws_secret_access_key[[:space:]]*=[[:space:]]*[A-Za-z0-9/+=]{40}' "AWS secret access key"
# GitHub PATs (classic + fine-grained)
scan_secret 'ghp_[A-Za-z0-9]{36}' "GitHub PAT (ghp_...)"
scan_secret 'github_pat_[A-Za-z0-9_]{82}' "GitHub fine-grained PAT (github_pat_...)"
# JWT shape
scan_secret 'ey[A-Za-z0-9_-]{10,}\.ey[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}' "JWT-shape token"
# Generic API-key heuristic — quoted assignments only (lower false-positive rate)
scan_secret '(api[_-]?key|secret|password|token)[[:space:]]*=[[:space:]]*['"'"'"][^'"'"'"]{8,}['"'"'"]' "hardcoded credential assignment"

# All checks passed — allow by absence
echo '{}'
```

`chmod +x hooks/pre-tool-use.sh`.

Constraints:
- Bash 3.2 portable — `case` patterns, no `[[ ... =~ ... ]]` (use grep -E for regex matching where needed)
- `jq -n --arg` for output (CR-02)
- Fast-path returns `{}` for non-matching commands and non-Bash tools
- No external dependencies beyond bash, jq, grep — already in PROJECT.md runtime budget
- Exemption-via-path-allow-list (D-06) is NOT implemented in this hook — by design, secrets ARE secrets regardless of file path. The exemption applies to test fixtures stored in `tests/`, but those don't get committed via `git commit` from the live working tree (Phase 5 bats fixtures use ephemeral repos). v2.0 keeps the scan strict; if false-positives surface in practice, externalize the exemption list in v2.1.</action>
  <verify>test -x hooks/pre-tool-use.sh
bash -n hooks/pre-tool-use.sh
shellcheck hooks/pre-tool-use.sh

# Functional tests
echo "=== Test 1: --no-verify blocked ==="
printf '{"tool_name":"Bash","tool_input":{"command":"git commit --no-verify -m foo"}}' | bash hooks/pre-tool-use.sh | jq -r '.hookSpecificOutput.permissionDecision' | grep -q deny && echo OK || echo FAIL

echo "=== Test 2: short -n blocked ==="
printf '{"tool_name":"Bash","tool_input":{"command":"git commit -n -m foo"}}' | bash hooks/pre-tool-use.sh | jq -r '.hookSpecificOutput.permissionDecision' | grep -q deny && echo OK || echo FAIL

echo "=== Test 3: force push to main blocked ==="
printf '{"tool_name":"Bash","tool_input":{"command":"git push --force origin main"}}' | bash hooks/pre-tool-use.sh | jq -r '.hookSpecificOutput.permissionDecision' | grep -q deny && echo OK || echo FAIL

echo "=== Test 4: AWS key blocked ==="
printf '{"tool_name":"Bash","tool_input":{"command":"export AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE"}}' | bash hooks/pre-tool-use.sh | jq -r '.hookSpecificOutput.permissionDecision' | grep -q deny && echo OK || echo FAIL

echo "=== Test 5: GitHub PAT blocked ==="
printf '{"tool_name":"Bash","tool_input":{"command":"echo ghp_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"}}' | bash hooks/pre-tool-use.sh | jq -r '.hookSpecificOutput.permissionDecision' | grep -q deny && echo OK || echo FAIL

echo "=== Test 6: normal Bash command allowed ==="
printf '{"tool_name":"Bash","tool_input":{"command":"ls -la"}}' | bash hooks/pre-tool-use.sh | grep -q '^{}' && echo OK || echo FAIL

echo "=== Test 7: non-Bash tool fast-path ==="
printf '{"tool_name":"Read","tool_input":{"file_path":"/tmp/foo"}}' | bash hooks/pre-tool-use.sh | grep -q '^{}' && echo OK || echo FAIL

echo "=== Test 8: normal git commit allowed ==="
printf '{"tool_name":"Bash","tool_input":{"command":"git commit -m foo"}}' | bash hooks/pre-tool-use.sh | grep -q '^{}' && echo OK || echo FAIL</verify>
  <acceptance_criteria>
    - "hooks/pre-tool-use.sh exists, is executable, shellcheck exits 0"
    - "All 8 functional tests above pass (5 deny patterns + 3 allow patterns)"
    - "Hook output is valid JSON (jq -e '.' on every test passes)"
    - "Hook never writes to source files — only reads stdin and emits stdout"
  </acceptance_criteria>
  <done>PreToolUse hook ships with both bypass blocker and secret scan working.</done>
</task>

<task type="auto">
  <name>Task 1.2: Wire PreToolUse binding in hooks/hooks.json (plugin-mode)</name>
  <files>hooks/hooks.json</files>
  <read_first>hooks/hooks.json, .planning/phases/03-hook-layer-expansion/03-CONTEXT.md (D-19, D-20)</read_first>
  <action>Edit `hooks/hooks.json` to add `PreToolUse` block (and PostToolUse — though PostToolUse hook itself is Plan 03-02; we add the binding now and create a no-op stub if needed; OR defer the binding to Plan 03-02. **Recommendation:** add only PreToolUse here; Plan 03-02 adds PostToolUse binding alongside its hook. Keeps each plan's diff scoped.)

Add to the `hooks` object:

```json
"PreToolUse": [
  {
    "matcher": "Bash",
    "hooks": [
      {
        "type": "command",
        "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/pre-tool-use.sh",
        "timeout": 5
      }
    ]
  }
]
```

The full file should become:

```json
{
  "description": "claude-godmode hooks — session context injection, post-compaction recovery, and quality-gate enforcement",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "timeout": 10
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/post-compact.sh",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/pre-tool-use.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
```</action>
  <verify>jq -e '.hooks.PreToolUse[0].matcher == "Bash"' hooks/hooks.json
jq -e '.hooks.PreToolUse[0].hooks[0].timeout == 5' hooks/hooks.json
jq -e '.hooks.PreToolUse[0].hooks[0].command' hooks/hooks.json | grep -q 'pre-tool-use.sh'</verify>
  <acceptance_criteria>
    - "hooks/hooks.json has PreToolUse block with matcher: Bash"
    - "Timeout is 5 seconds"
    - "Command points at pre-tool-use.sh via $CLAUDE_PLUGIN_ROOT"
    - "JSON is valid (jq -e '.' passes)"
  </acceptance_criteria>
  <done>Plugin-mode binding wired.</done>
</task>

<task type="auto">
  <name>Task 1.3: Wire PreToolUse binding in config/settings.template.json (manual-mode) + add timeouts</name>
  <files>config/settings.template.json</files>
  <read_first>config/settings.template.json, hooks/hooks.json, .planning/phases/03-hook-layer-expansion/03-CONTEXT.md (D-18, D-19)</read_first>
  <action>Edit `config/settings.template.json` to:
1. Add `"timeout": 10` to existing SessionStart and PostCompact bindings (currently missing — Phase 1 left this gap; Phase 3 fixes it as part of HOOK-06)
2. Add PreToolUse binding (same shape as plugin-mode, with `~/.claude/` path prefix instead of `${CLAUDE_PLUGIN_ROOT}`)

Resulting `hooks` block in settings.template.json:

```json
"hooks": {
  "PostCompact": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "bash ~/.claude/hooks/post-compact.sh",
          "timeout": 10
        }
      ]
    }
  ],
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "bash ~/.claude/hooks/session-start.sh",
          "timeout": 10
        }
      ]
    }
  ],
  "PreToolUse": [
    {
      "matcher": "Bash",
      "hooks": [
        {
          "type": "command",
          "command": "bash ~/.claude/hooks/pre-tool-use.sh",
          "timeout": 5
        }
      ]
    }
  ]
}
```</action>
  <verify>jq -e '.hooks.SessionStart[0].hooks[0].timeout == 10' config/settings.template.json
jq -e '.hooks.PostCompact[0].hooks[0].timeout == 10' config/settings.template.json
jq -e '.hooks.PreToolUse[0].matcher == "Bash"' config/settings.template.json
jq -e '.hooks.PreToolUse[0].hooks[0].timeout == 5' config/settings.template.json</verify>
  <acceptance_criteria>
    - "config/settings.template.json has timeout: 10 on SessionStart and PostCompact"
    - "config/settings.template.json has PreToolUse block with matcher: Bash and timeout: 5"
    - "JSON is valid"
  </acceptance_criteria>
  <done>Manual-mode binding wired; parity ready for QUAL-03 assertion.</done>
</task>

</tasks>

<verification>
- [ ] All 3 tasks pass their checks
- [ ] Both config files declare PreToolUse with matcher: Bash, timeout: 5 (path prefix differs only)
- [ ] 3 atomic commits
</verification>

<success_criteria>
- HOOK-01, HOOK-02 closed
- HOOK-06 substrate started (Plan 02 finishes the parity for PostToolUse)
- pre-tool-use.sh refuses 5+ patterns; allows normal commands
- jq-only output (no heredoc); CR-02 regression-free
</success_criteria>

<output>
`.planning/phases/03-hook-layer-expansion/03-01-SUMMARY.md`
</output>
