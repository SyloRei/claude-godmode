.DS_Store
node_modules/
*.bak

# claude-godmode pipeline artifacts (v1.x runtime state)
.claude-pipeline/

# Planning artifacts kept local (commit_docs=false in config.json)
# Already-tracked files under .planning/ stay tracked; this prevents
# accidental staging of new briefs, plans, and per-brief work artifacts.
.planning/
